# Convenções

**Framework DOX:** hierarquia de `AGENTS.md` como contrato de trabalho por subárvore (skill `~/.claude/skills/dox/`). Antes de editar, ler a cadeia DOX da raiz até o alvo (root `AGENTS.md` + cada `AGENTS.md` no caminho); o mais próximo é o contrato local. Após mudança significativa, atualizar o `AGENTS.md` dono mais próximo + pais/filhos afetados + Child DOX Index (DOX pass no closeout).

Árvore atual: root, `src/tdt`, `src/tdt/{scoring,matchers,dados}`, `tests`, `bench`, `docs`. Não confiar na memória — reler a cadeia a cada sessão (pode ter mudado).
