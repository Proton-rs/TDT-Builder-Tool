# AGENTS.md — docs

## Purpose
Materiais de origem (pipeline, inputs, templates, lista padrão) e specs do projeto.

## Local Contracts
- `superpowers/specs/`: specs de design (formato brainstorming). Implementar via `superpowers:writing-plans` → `executing-plans`, ou "implemente <spec> via TDD".
  - `2026-06-23-sp1-backbone-deterministico-design.md` (SP1 base — implementado)
  - `2026-06-23-sp1-melhoria-analise-design.md` (calibração + e5 + consenso + opostos)
  - `2026-06-23-sp1-melhoria-motor-regras-design.md`
  - `2026-06-23-sp1-melhoria-normalizacao-design.md`
  - `2026-06-24-sp4-ui-desktop-design.md` (SP4 UI Desktop — implementado)
  - `2026-06-24-sp4-ui-correcoes-design.md` (SP4 correções de usabilidade — implementado)
  - `superpowers/specs/2026-06-24-sp7-analise-lista-homogenea-deterministica-design.md` (SP7 caminho determinístico p/ lista homogênea — implementado)
  - `superpowers/specs/2026-06-24-sp8-ui-filtros-tela-inicial-pendente-design.md` (SP8 UI: filtros por coluna, abas, status pendente — implementado)
  - `superpowers/specs/2026-06-24-sp9-tdt-completude-usabilidade-design.md` (SP9 TDT: Output Coordinates, dropdowns, Measurement Type/Display Unit — implementado)
   - `superpowers/specs/2026-06-24-sp10-djf1-pareamento-polaridade-design.md` (SP10 DJF1 + pareamento de polaridade — implementado)
   - `superpowers/specs/2026-06-25-sp-revisao-ui-design.md` (Revisão UI: pareamento D+C, equipamento colunas, add/remover, filtro módulo, relatório formatado — spec, não implementado)
   - `superpowers/specs/2026-06-25-sp-tdt-input-coordinates-format.md` (TDT: Input Coordinates numérico — implementado)
   - `superpowers/specs/2026-06-25-sp-performance-pipeline.md` (Performance: batch encoding, scorer cache, lazy encoder, progresso UI, profiling — spec)
   - `superpowers/specs/2026-06-25-sp-analise-qualidade-matching.md` (Análise: tabela de qualidade, estatísticas, relatório exportável — spec)
   - Decomposição das observações 26/06 (`observacoes26062026.md`) em 4 specs (cada uma seu ciclo spec→plano):
     - `superpowers/specs/2026-06-26-spA-revisao-ui-lote-enderecamento-modulo-design.md` (A: edição em lote, undo/redo, colunas End.Input/Output/Pareado, módulo+tipo editáveis, travar visão, reordenar colunas — spec; §1.1/1.2/1.3/1.4/2.1/4.4)
     - `superpowers/specs/2026-06-26-spB-correcoes-scoring-pareamento-design.md` (B: confiança ausente/calibrada, motor de regras como filtro pós-normalização, DCpairer robusto, fusão de duplicados — spec; §2.2/3.1/3.2)
     - `superpowers/specs/2026-06-26-spC1-identidade-tipo-modulo-design.md` (C1: identidade real do módulo + classificação de tipo, determinístico — spec; §4.2/4.3). Raiz da decomposição C.
     - `superpowers/specs/2026-06-26-spC2-inferencia-equipamento-topologia-design.md` (C2: inferir equipamento pela topologia típica do módulo — spec; §4.1; depende de C1, alimenta guard-rail da B)
     - `superpowers/specs/2026-06-26-spC3-mineracao-full-base-design.md` (C3: minerar Export Full Base → catálogo de sinais por contexto + mapa Measurement Type, offline → artefatos JSON — spec; §5.1/5.3)
     - `superpowers/specs/2026-06-26-spC4-contexto-estendido-planilha-design.md` (C4: capturar título/observações/células superiores da sheet como contexto — spec; §5.2)
     - `superpowers/specs/2026-06-26-spD-qualidade-saida-design.md` (D: não sobrescrever saída, gate de falsos positivos, corpus adversarial — spec; §6.1/6.2/7.1)
   - `superpowers/specs/2026-06-26-spE-treinamento-calibracao-probabilistica-design.md` (E: calibração por-método + pesos de mescla aprendidos contra correção + mescla probabilística + troca e5 + calibrador Platt/isotonic — spec; eixo de treinamento; dona das pendências de calibração que B2/C deixaram em aberto)
   - `superpowers/specs/2026-06-26-lista-adms-v6-enriquecimento-por-auditoria-design.md` (v6: descrições enriquecidas com variantes reais mineradas de `Auditoria_Revisao.xlsx`, por sigla e sem boilerplate compartilhado, para matching — spec)
   - `superpowers/specs/2026-06-29-sp-categoria-dual-pass-fix-design.md` (Categoria: barreira de domínio no dual-pass — elimina 213 FPs de categoria trocada — **implementado**)
   - `superpowers/specs/2026-06-29-sp-ancoragem-sigla-explicita-design.md` (Ancoragem: detecta sigla explícita na descrição → injeta candidato + filtrar_subarvore limita ao sub-ramo da âncora; resolve 132 erros de família errada — **implementado**)
   - `superpowers/specs/2026-06-29-sp-pareamento-double-bit-posicao-design.md` (Pareamento double-bit: catálogo de 9 siglas de posição; pareamento_polaridade generalizado para Seccionadora (7 SEC* por palavra-função) + DJA1; posicao_ambigua para sem keyword — **implementado**)
   - `superpowers/specs/2026-07-15-sp-device-mapping-rge-design.md` (Identidade de equipamento + Device Mapping padrão RGE: whitelist `(52|24|29|89)-N`+`TR{n}` no N0, varredura da linha inteira + `equipamento_conflitante`, registro de equipamentos por módulo, regras RGE de Device Mapping discreto/analógico — **implementado**)
   - Planos de implementação correspondentes em `superpowers/plans/2026-06-24-sp{7,8,9,10}-*.md`.
   - `superpowers/specs/2026-07-15-sp-ux-perf-ferramenta-design.md` (SP-UX-PERF: marcação de sheets em grupo, filtro por coluna pós-fechamento, geração seletiva por módulo, Equipamento/Descr. bruta editáveis, remoção em massa reset/ranges — **implementado**)
- Fontes de verdade de dados: `Pontos Padrao ADMS_v2.xlsx` (lista padrão de **ANÁLISE/matching**, **default** — descrições da v1 + fixes de campo + DJF1 enriquecido; descrições limpas, sem diluição). `Pontos Padrao ADMS_v5.xlsx` = mesma base v2 com a coluna `DESCRIÇÃO NOVA` **enriquecida** (append-only, ANSI C37.2 + domínio) para **DESCRIÇÕES/referência humana** — NÃO usar para matching (o texto compartilhado dilui a discriminação; comprovado por regressão no benchmark: acc 82→68%, prec 95→68%). v1/v3/v4 = histórico, não editar. `dnp3_template.xlsx` (template TDT), `input_*.xlsx` (exemplos), `Export_base_Full*.xlsx` (base real com multiplas Subestações e padrões de escrita diferentes, 97MB — não abrir inteiro).
- Não editar os `.xlsx` de origem; são insumos.

## Ledger de decisões arquiteturais
Decisão registrada ≠ estado do código. Antes de afirmar "foi decidido X, então o código faz X", conferir aqui. Atualizar no closeout de cada SP (junto com o DOX pass): toda decisão de spec ganha uma linha com status `implementado | pendente | revertido | superado`.

| Decisão | Spec/commit | Status no código |
|---|---|---|
| B2: motor de regras como filtro bounded (clamp+renormaliza, deltas em [0,1], recalibrar thresholds) | spB 26jun §B2 | **pendente por decisão** — SP-Unificado 08jul: usuário optou por pular a Fase 5 (alto risco de regressão; sintoma >100% já resolvido pelo clamp de exibição). Deltas seguem unbounded. Parcialmente superado: E4 (calibrador platt pós-mescla) + clamp de exibição no boundary (08jul) |
| Remoção de candidatos contraditórios | filtro_preciso (F1, SP-G Task 6) | implementado — módulo próprio; motor_regras nunca remove |
| TFIDF → BM25 | SP-H 03jul | implementado (com fix de dedup de sigla duplicada) |
| type_severidade no corpus vetorial | SP-METADADOS Task 5 | **revertido** (36c2b68) — regrediu o gate |
| Stemmer N6 | 26jun | **gateado off** — regrediu bench |
| Lista v5 (descrições enriquecidas) para matching | 26jun | **rejeitado** — diluição comprovada (acc 82→68%); matching usa v2 |
| B1: confiança de decisão por regra (candidatos=() → UI vazia) | spB 26jun §B1 | implementado 08jul (display-only: "1.00 (regra)" em modelo_tabela/tela_revisao) |
| Gate `equipamento_ambiguo` (C2) | spC2 26jun | **superado** — Spec C: dc_pairer arbitra (sem-comando→TDT, comando→pareamento_ambiguo); teste garante que o motivo NÃO aparece |
| e5/reranker (troca de embedding) | spE 26jun / spD3 01jul | **rejeitado** (3ª rodada, empate com MiniLM e modelo 1GB maior); capacidade dorme em `dados/encoder`+`indice_vetorial` (param `prefixo`); `config.e5_prefixos` é o knob dormente (bench/diag usa) — não remover |
| `matchers/cross_encoder.rerank` | SP-G | **dormente** — implementado, só teste usa; wiring pendente de decisão (mesma família do e5) |
| 7 regras SE/ENTÃO propostas (79LO/86, SF6 estágios, 50BF, mola→BB*, CDC/OLTC, SECG, sincronismo) | conhecimento_sinais.md §"Itens acionáveis" / SP-Unificado 08jul | **fechadas** — item 3 (f_sf6, gate 66.9→67.4), item 6 (f_79lo), item 1 (f_50bf), item 2 (mola→bobina) implementados individualmente com gate ≥ baseline; item 4 (SECG) já coberto (status sozinho não é gap), item 5 (CDC/OLTC) já coberto (normalizador), item 7 (sincronismo) já coberto (especificidade_qualificador) |
| `f_posicao` fora do registro `_FILTROS` | SP-G Task 6 | **deliberado** — aplicado separado (filtro_preciso:226); registrar duplicaria aplicação |
| spC3 (mineração full base), spE2 (mescla probabilística/pesos aprendidos) | specs 26jun | **pendente** — nunca viraram plano; specs grandes, cada uma exige ciclo spec→plano→gate |
| spC4 (contexto de topo da sheet no corpus) | spC4 26jun / SP-Unificado 08jul | **testada, revertida** — implementada e medida; contexto constante por sheet dilui embedding: gate 67.4→50.1. Arquivada (bench/resultados/spUNI_spC4.txt) |
| spD (corpus adversarial anti-FP) | spD 26jun / SP-Unificado 08jul | **implementado** — tests/corpus_adversarial.py trava invariantes de normalização (religamento≠desligamento, SGF/ATUADO, fase A, mola→bobina); 50F1/51N1 truncadas ficam xfail (gate real: casos_travados.csv) |
| Dataclass `Pareamento` (contrato tipado D+C p/ UI) | SP-REVISAO-UI / SP-Unificado 08jul | **implementado** — contracts.py (frozen); lógica de pareamento segue em dc_pairer (não alterado) |
| Classificação de direção/comando | D1-D4 / SP-Unificado Fase 8 | **bloqueada por dado** — diagnóstico 08jul: entrada não sinaliza direção textualmente (156/243 comandos reais têm texto de puro status). Direção é propriedade de família de Message Mapping, não regra de texto. Decisão de design pendente do usuário (docs/superpowers/specs/2026-07-08-direcao-comando-diagnostico.md) |
| Categoria DiscreteAnalog (TAP) — 3ª aba da lista padrão + sheet de saída | SP-Pendencias-09jul Fase 3 (Tasks 6-8) | **implementado** — v7=v2+aba DiscreteAnalog (default→v7; matching v2 intacto); `lista_padrao` lê a aba (+5 campos, retrocompat); `engine_tdt.gerar` roteia por sigla (3-vias) p/ `DNP3_DiscreteAnalog`; hoje só TAP (exceção só em transformadores) |
| Lista v7 = v2 + aba, NÃO v6 | SP-Pendencias-09jul 09jul (decisão usuário) | **implementado** — basear em v6 adotaria descrições enriquecidas que mudam o matching de todos os sinais (fora de escopo + confunde gate); v7 preserva matching da v2 |
| Auto-tuning de pesos de mescla | SP-Pendencias-09jul Fase 5 (Task 10) | **MANTER 0.70/0.25/0.05** — `bench/tune_pesos.py` (grid simplex + validação no gate real): nenhum combo supera o atual em acertos absolutos no gate (atual iguais=649 vs 626/626/613). Estágio-1 (ROTULOS) favorece rebalancear mas não generaliza. Config inalterado; combos de maior *pct* (menos decididos) são trade-off de produto (follow-up) |
| Remote Point Alias / measurement types / device mapping homogêneo / nomes de arquivo | SP-Pendencias-09jul Fase 1-2 (Tasks 1-4) | **implementado** — alias `%Y%m%d`; `_MEASUREMENT_TYPE_PT_EN` cobre 12 tipos (KMDF→Unitless); módulo homogêneo compõe tipo+nº operativo do header (AL+23=AL23); nomes `TDT_<SUB>_<YYYYMMDD>[_vN].xlsx` (`nomes_saida`) |
| Boot da UI lazy-load (encoder + pipeline) | SP-Pendencias-09jul Fase 4 (Task 9) | **implementado** — `worker.py`/`ui_main.py`/`tela_geracao.py` não importam encoder/pipeline no topo; boot 17.8s→1.18s; teste de regressão `test_boot_sem_transformers` (subprocess) trava transformers/sklearn fora do boot |
| Gate `gate_tdt_real` Input Coordinates por sheet | SP-Pendencias-09jul Task 8 | **parcial (deliberado)** — DiscreteSignals=31, DiscreteAnalog=34 (índices reais). AnalogSignals mantido em 31 (índice real=47) p/ preservar baseline; **FOLLOW-UP**: analógicos são silenciosamente excluídos do gate (bug latente pré-existente) e o keying por endereço achatado colide entre categorias (TAP@1023 vs discreto) |
| 10 siglas homogêneas faltando na lista padrão (FGOO, 86RM, família TCT/81) | SP-Pendencias-09jul Task 5 (diagnóstico) | **FOLLOW-UP** — `bench/diag_pendentes_homogeneo.py` classificou; adicionar à lista padrão exige dado de domínio (desc/signal type/MM/severidade), não fabricar. **Hipótese do usuário (09jul): parte pode ser erro de nomenclatura na ORIGEM, não sigla nova — ex. `FGOO` (23 ocorrências, "FALHA GOOSE") provavelmente deveria ser `GOOS`/`GOOSE`.** O fuzzy (b) não pegou porque comparou contra a lista padrão, não contra o vocabulário canônico esperado; a correção certa pode ser normalizar a sigla de entrada (FGOO→GOOSE) em vez de adicionar FGOO à lista. Confirmar caso a caso com o proprietário antes de decidir (normalizar vs adicionar) |
| Correção import IMA: nome por equipamento no homogêneo, gate custom ID, lista v8, COMTAP→revisão, tipo `A/D` | Correção-Import-IMA 10jul (Tasks 1-5) | **implementado** — `normalizacao/identidade_homogenea.py` (novo, resolve módulo/equipamento do caminho homogêneo pelo bloco de cabeçalho EQUIPAMENTO/NÚMERO OPERATIVO, validado contra oracle real 1400/1404); `estruturador_homogeneo.estruturar_homogeneo()` agora retorna 4-tupla `(decididos, pendentes, revisao, avisos)`, usa o resolver acima, roteia COMTAP (e demais `config.siglas_sem_ponto`) pra `revisao` (motivo `comando_tap_nao_modelado`, em vez de gerar sinal — base real tem 0 sinais COMTAP), mapeia tipo `A/D` → `DiscreteAnalog`/`Input`, e loga lint não-bloqueante quando o NOME do cliente diverge do calculado; `engine_tdt.particionar_custom_id_duplicado()` (novo gate, workbook-wide) tira grupos inteiros de Remote Point Custom ID colidente da lista e manda pra `revisao` (motivo `custom_id_duplicado`) antes de `engine_tdt.gerar`, wired em `pipeline.gerar_tdt` e `pipeline.executar`; `Pontos Padrao ADMS_v8.xlsx` corrige MM de `43LR` e `81U1`-`81U5` (únicas células divergentes da v7) e vira `defaults.DEFAULT_LISTA`, com teste de domínio novo garantindo que todo MM da lista padrão existe no catálogo real (`tests/fixtures/mm_catalogo_real.txt`); `scripts/verificar_aceite_ima.py` (evidência, não produção) comprova zero duplicidade de Custom ID, MMs em catálogo, ambos os TAP como DiscreteAnalog e COMTAP em revisão no TDT da IMA regenerado |
| `config.piso_decisao` — piso absoluto de confiança calibrada (0.20) | SP-CVA 13jul Task 5 (db1ca6a) | implementado — `roteador._quadrante` manda pra revisão (`score_baixo`) quando a confiança calibrada do top-1 fica abaixo do piso, mesmo com gap grande; protege contra decisão tipo 51F→FC87 (score cru falso positivo) |
| Motivo `modulo_duplicado_entre_sheets` | SP-CVA 13jul Task 6 (71a2dd1) | implementado — `particionar_custom_id_duplicado` diferencia colisão de Custom ID entre sheets distintas (dado de origem com módulo errado) de colisão dentro da mesma sheet (mantém `custom_id_duplicado`) |
| `f_r4` fallback de estágio (50N E2) | SP-CVA 13jul Task 4 (82d2068) | implementado — só remove candidato sem-estágio se a família tiver variante do estágio certo (antes removia sempre, mesmo sem variante concorrente) |
| Fases CA↔AC (extração + boundary de saída) | SP-CVA 13jul Task 4b (4b8f1bd) | implementado — convenção interna continua `CA`; `engine_tdt._fase_saida` traduz pro domínio `PhaseCode` do template ADMS (`AC`) só na saída; extração (`normalizador._fase_no_texto`) reconhece pares alfabéticos AC/BA/CB na entrada e canoniza pro interno |
| Par de posição na UI (`formar_par_posicao`/`trocar_sigla_par`) | SP-CVA 13jul Task 15 (f1283ea) | implementado — caminho do operador em `AppState` pra corrigir DJA1→DJF1 fundindo dois registros em MultiCoord (ou trocar a sigla de um par já fundido) |
| ID de equipamento malformado (`O` no lugar de `0`) | SP-CVA 13jul Task 8.2b (0f5038a) | implementado — `_ID_LETRA_NUM` aceita `O` como typo de `0` na posição final (regex específica, não afrouxamento geral do reconhecedor) |
| Comando "Abrir/Fechar" converge pra sigla do status forçado | SP-CVA 13jul Task 8.1 (974dca9) | implementado — `forcar_polaridade_equipamento` passa a reconhecer comandos de uma linha só (sem par status+comando explícito) |
| Status Tasks 8/9 (correções de pareamento CVA + VAB CVA11) | SP-CVA 13jul Fase 5/3 | Task 8.1 e 8.2(b) implementadas, gate individual OK; Task 9 implementada — inferência de grandeza contínua por descrição quando não há coluna TIPO nem marcador de seção (layout CVA11) |
| Par de posição por particípio exato (ruído ABERTURA) | SP-CVA2 E1 14jul (a128867) | implementado — `pareamento_polaridade._polaridade_pura` pareia por palavra EXATA de particípio, não mais prefixo |
| Reconciliação comando↔status de posição no dc_pairer | SP-CVA2 E2 14jul (befbd90) | implementado — `dc_pairer._reconciliar_posicao` re-chaveia sigla de posição divergente pra sigla do status (se inequívoco); motivo novo `posicao_divergente` quando ambíguo |
| Marcador de seção tolera numeração + códigos AI/AO/DI/DO + grandeza por token (CVA11) | SP-CVA2 E3 14jul (773fb1e, 2928a2f, 9ae0892) | implementado — `estruturador._eh_marcador` tolera célula de numeração de sequência; `vocabulario_tipo.CODIGOS_TIPO` ganha AI/AO/DI/DO; `_col_tipo` troca issubset estrito por interseção ≥2; `_grandeza_continua` por token exato + guarda FALTA/PERDA |
| Fusão MultiCoord do par de posição ANTES do dc_pairer | SP-CVA2 E4 14jul (c74764e) | implementado — `normalizador_estrutural.fundir_pares_posicao` nos 2 call sites do pipeline; resolve caso "par + comando" que caía no catch-all greedy do pairer |
| Sanitização módulo-por-linha + aviso divergência sheet×conteúdo | SP-CVA2 E5 14jul (3a40fa9, 47edc81) | implementado — `identidade_modulo.ResolucaoModulo.canonico` saneia módulo fora do padrão pro dominante da sheet (com aviso); `aviso_divergencia_sheet` (BC2 rotulada BC1 na origem); `aplicar_identidade` passa de 2-tupla pra 3-tupla `(sinais, confianca, avisos)` |
| Conservação total (não só comandos) + gate endereço duplicado escopado por módulo | SP-CVA2 E6 14jul (cc68c98, 3b1ed5c, 25fcca4) | implementado — `test_conservacao_comandos.py` cobre todas as direções; `engine_tdt.particionar_endereco_duplicado` (escopado por módulo, não workbook-wide — 1ª tentativa sem módulo quebrou testes com múltiplos IEDs reusando índice local); UI: "Pareado" mostra "Comando (sem par)"; label/tooltip do motivo `posicao_divergente` |
| Guarda de direção Input no predicado `fundivel` do normalizador estrutural | SP-CVA2 follow-up 14jul (b180022) | implementado — `normalizador_estrutural.corrigir()`: `fundivel` ganhou guarda de direção Input, unificando com `fundir_pares_posicao` (achado no code review da Task 6) |
| Marcação de sheets em grupo (context menu Marcar/Desmarcar/Inverter + Espaço + hitbox de linha inteira) | SP-UX-PERF Task 1 15jul (e02b51a) | implementado — `tela_inicial.definir_marcacao`/`inverter_marcacao` (módulo-level, agem nas linhas selecionadas; sem seleção, em todas); `_sheet_alterada` continua único escritor de `sheets_excluidas`/aliases |
| Filtro por coluna aplica pós-fechamento do dialog + caminho quente sem QModelIndex | SP-UX-PERF Task 2 15jul (7dc36c5) | implementado — `_filtrar_coluna` fecha o dialog e aplica via `QTimer.singleShot(0, ...)` com cursor busy; `ModeloSinais.valor_texto(row, col)` espelha o branch DisplayRole de `data()`, usado em `proxy_revisao.filterAcceptsRow`/`valores_unicos`; `super().filterAcceptsRow` (busca global) reordenado pro final (mais caro) |
| Geração seletiva por módulo | SP-UX-PERF Task 3 15jul (e333fa1) | implementado — `tela_geracao.filtrar_por_modulos` (pura); lista de módulos checkable, todos marcados por default; TDT, relatório e avisos (`_montar_avisos`) operam sobre o MESMO subconjunto filtrado; sem módulo marcado → botão Gerar desabilitado |
| Equipamento (ID) e Descr. bruta editáveis na revisão | SP-UX-PERF Task 4 15jul (f27932c) | implementado — `AppState.definir_equipamento`/`definir_descricao_bruta` via `_editar_nested` (undo padrão); `DelegateEquipamento` sugere IDs já usados no mesmo módulo, aceita texto livre, vazio limpa; Descr. bruta rejeita vazio, NÃO reprocessa normalizada/tokens |
| Remoção em massa: reset único (>100 linhas) / ranges contíguos (≤100) + `dataChanged` agregado no lote | SP-UX-PERF Task 5 15jul (08d0876) | implementado — `remover_linhas` roteia por `LIMIAR_RESET_REMOCAO=100`; ranges processados em ordem reversa (evita corromper índices ainda pendentes); `aplicar_valor_em_lote` suprime snapshot/dataChanged por linha e emite um agregado no fim; `_aplicar_em_lote` (tela_revisao) suspende repaint via `setUpdatesEnabled(False)` |
| Rename de sheets em lote | SP-UX-PERF 15jul | **descartado por decisão do usuário** — rename continua um a um, inline |
| Whitelist de equipamento `(52\|24\|29\|89)-N` + `TR{n}`; 24=Disjuntor (decisão do usuário 15/07) | SP-DEVICE-MAPPING-RGE 15jul (1b1e167) | implementado — `normalizador._ID_EQUIPAMENTO` restrito à whitelist RGE (era `\d+-\d+` genérico, falso-positivo em `81-1` etc.); `familia_do_id` público, consumido pelas Tasks 3 e 5 |
| Varredura de equipamento na linha inteira (caminho heterogêneo) + motivo `equipamento_conflitante` | SP-DEVICE-MAPPING-RGE 15jul (14a886b) | implementado — `estruturador.estruturar` varre todas as células da linha (exceto descrição/módulo) via `normalizador.equipamentos_no_texto`; 2+ IDs distintos na linha → `revisao`, nunca descarte; mesmo ID repetido não conflita |
| Registro de equipamentos por módulo (`atribuir_id_por_registro`) | SP-DEVICE-MAPPING-RGE 15jul (59d8912) | implementado — `inferencia_topologia.atribuir_id_por_registro`, wired em `pipeline.py` logo após `inferir_equipamento`; preenche `nome_equipamento` quando a família do sinal tem exatamente 1 ID achado na sheet; 2+ → aviso `registro_equipamentos` (1 por módulo+família), nunca inventa ID |
| Device Mapping padrão RGE (discreto sem sigla em não-proteção; analógico TC/TP/disjuntor; fallback módulo duplicado) | SP-DEVICE-MAPPING-RGE 15jul (275a720, b11bb03) | implementado — `engine_tdt._device_mapping` (discreto) + `_device_mapping_analog`/`_disjuntor_por_modulo` (analógico), único choke point p/ homogêneo e heterogêneo; só a coluna Device Mapping muda, Custom ID/nome_hierárquico intactos; analógico sem regra específica cai no disjuntor do módulo (decisão do usuário 15/07) |
| Módulo-por-coluna e sigla-por-coluna independentes (regressão LVA AL21) | Correção 16jul (87a347b) | implementado — `estruturador.estruturar`: `elif` de b9b0118 fazia a coluna MODULO reivindicada desligar a pré-classificação por sigla da sheet inteira (1 célula divergente em 83 mudava tudo); ramos agora independentes, precedência só dentro da mesma identidade; 3 testes de guarda em `test_estruturador.py` |
| Regra universal de não-regressão + fluxo de dados transparente | SP-FLUXO-DADOS-TRANSPARENTE 16jul (2d8b23c, 081ebbb, 0fd8f36, 2ff6d6e, 1d77e79, b8ee97f, 37f3df3, 01253d7) | implementado — regra no `CLAUDE.md`; spec `2026-07-16-sp-fluxo-dados-transparente-design.md` (I1–I4); `docs/fluxo_dados.md` (mapa etapa×le/escreve/sobrescreve); `Auditoria.sobrescritas`+`diff_identidade` (auditoria.py, duck-typed) classificam mudança de identidade em `sobrescrita`(INFO)/`perda`(AVISO); wired em `gerar_tdt` e `executar` (fundir_pares_posicao, dc_pairer, corrigir, montar, aplicar_identidade, subdividir_at_bt); `test_conservacao_identidade.py` (zero perdas, cadeia parcial) + 3 testes I2 independência sigla/módulo×equipamento em `test_estruturador.py`; `scripts/relatorio_fluxo_real.py` (gate de closeout) — 0 PERDAS em LVA (317 eventos) e CVA (1370 eventos), suíte 1026 passed/2 xfailed |
| Aprovar não reverte sigla reclassificada pelo usuário | Correção 16jul (16645f4) | implementado — `_aprovar_e_proximo` (single-row) e `AppState.aprovar_ids` (lote) pegavam `candidatos[0]`/currentItem do painel por cima da sigla já editada; prioridade agora: `sigla_sinal` atribuída > painel > candidato top; teclas 1-5 seguem explícitas; registro sem candidatos mas com sigla passa a ser aprovável (era beep) |
| P4 — `config_io` persiste só deltas vs `Config()` default + migra trio de pesos stale (`0.34/0.33/0.33`→`0.70/0.25/0.05`) + guard soma pesos + marcador de override na UI | SP-OBS-17JUL 18jul (363de32, 4a5e250, 4b69971) | implementado — `salvar_config` só grava `[config]`/`[pesos_regras]` quando divergem do default; `carregar_config` descarta o trio stale só quando os 3 batem juntos (override legítimo de 1/2 preservado); confirmado contra o `config.toml` REAL do usuário (tinha o trio stale) — antes desta fix a UI rodava silenciosamente com pesos descalibrados a cada boot |
| P1 — endereços exibidos/editados por direção (`Endereço Input`/`Endereço Output`) | SP-OBS-17JUL 18jul (8d144b4, cdc6677) | implementado — `modelo_tabela.enderecos_exibicao`; Output puro mostra endereço próprio de escrita na coluna Output, Input "—"; `_valor_edicao`/painel de detalhes corrigidos p/ não vazar sentinela "—" no round-trip de edição (achado em revisão) |
| P3 — reparear em lote (`AppState.aplicar_reparear` + botão "Reparear sheet") | SP-OBS-17JUL 18jul (198c063, ffa5331) | implementado — núcleo puro `ui/reparear.py` reusa `dc_pairer.parear`+`fundir_pares_posicao`; 1 snapshot p/ o lote inteiro; coluna Motivo ganhou fallback pra `rec.justificativa` (achado necessário — prereq de P3b também) |
| P3b — separar par de posição (`AppState.separar_par_posicao`, reconstrução) | SP-OBS-17JUL 18jul (0db794a) | implementado — inverso de `formar_par_posicao`; RECONSTRÓI 2 Inputs de 1 endereço (não desfaz — dado absorvido na fusão original não existe mais, decisão spec §9.6); marca `justificativa="posicao_ambigua"` p/ redecisão manual; **FOLLOW-UP**: preservar dado absorvido na fusão original (fora de escopo deste plano) |
| 2A — canonização de sigla via `DE->PARA` (loader + choke points) | SP-OBS-17JUL 18jul (928d16c, 3c95505) | implementado — `ListaPadraoADMS.de_para`; aplicado em `estruturador`/`estruturador_homogeneo` COM precedência (só canoniza sigla bruta que ainda não resolve direto) — sheet real tem entradas de siglas JÁ válidas (LDF→MANUT, FUGA→FGTE) que quebravam SAN2 sem a guarda; gate OK (pct=72.3, mesmos 6 FAILs) |
| 2D — coluna Severidade (metadado, não-scoring) | SP-OBS-17JUL 18jul (e613a33) | implementado — `SinalPadrao.severidade` + coluna não-editável em `modelo_tabela`; nunca entra em scoring/corpus (grep confirmado); **nota operacional**: `Pontos Padrao ADMS_v8.xlsx` real tem SEVERIDADE como fórmula sem cache salvo — coluna aparece vazia até o xlsx ser resalvo no Excel |
| 2C — catálogo Message Mapping: validação na carga (uso1) + fallback do filtro D2 (uso2) | SP-OBS-17JUL 18jul (dff2988, be1d242) | implementado — `catalogo_mm`/`validar_mm` (avisos, wired no canal de avisos do pipeline); `filtrar_por_estado` usa MM como fallback quando FUNÇÃO (sheet) está silenciosa — FUNÇÃO sempre prevalece, divergência gera aviso; **FOLLOW-UP**: `divergencias_funcao_mm` é função pura sem call site em produção (aviso da spec §9.4 ainda não observável em nenhum canal real) |
| P5 — diagnóstico de âncora→revisão + C1 (variante-pai exata) + C4 (seleção por classe de estados MM) + C3 (resgate de família filtrada) | SP-OBS-17JUL 18jul (5fd92e0, eae38aa, d7e61ce, dc943d4, ec72813, 89549e8) | implementado — diagnóstico (Task 11): H1 confirmada 75/93, H2 não confirmada 0/93 (**C2 pulada**), H3 confirmada 83/93, partição LP por `classe_do_mm` confirmada parcial (família 81 precisa fallback de estágio). C1: `ancoragem_sigla.desambiguar_variante` decide variante-pai quando âncora exata sobrevive ao top-3 mas empatada com irmãs; motivo `variante_ambigua`. C4: mesma função, seleção positiva por `classe_do_mm`+estágio quando família inteira precisa ser expandida da LP (não exige âncora no top-3); caso 81U1 coberto por teste sintético (sem caso real roteável nas 5 listas disponíveis — não fabricado em `casos_travados.csv`). **C3 (fora do plano original de 21 tasks — aberto ao vivo por decisão do usuário 18/07, gate §"C3" do plano p/ H3 confirmada)**: `ancoragem_sigla.resgatar_familia_ausente` reinjeta âncora numérica exata cuja família inteira sumiu do top-3 pós-filtros-semânticos (não do pool completo — 1ª versão conservadora demais, corrigida ao vivo pelo arquiteto); guarda numérico-only mantém SF6/VF* (não-numéricos) intocados; resolveu o caso referência real (FWB "CMD BLOQ 87B") + 1 caso adicional (20C); 9 fires pré-existentes preservados; **FOLLOW-UP não-bloqueante**: resgate roda ANTES de `motor_regras.aplicar_rastreado`, então vê o top-3 pré-regras, não o top-3 final pós-regras que o roteador de fato usa — pode reproduzir a mesma classe de bug 1 fase depois (não observado, sem teste hoje). Gate OK em todas as rodadas (pct=72.3, mesmos 6 FAILs conhecidos) |
| 2B — FASES estruturada da LP p/ analógicos (`r10_fase_lp`) | SP-OBS-17JUL 18jul (9bea641, 7aa05c2) | implementado — `SinalPadrao.fases` + regra nova; **bug crítico achado em revisão e corrigido**: comparação por substring quebrava o par de fase A-C (LP monta "AC" alfabético, texto canoniza "CA" — `normalizador._PAR_FASE_ALFABETICO`) e penalizava incorretamente sinais reais (4 linhas reais com `FASES="L1 L3"`); corrigido pra comparação de conjunto (`set<=set`), ordem-independente |
| 2E (MANUT_*) / 2G (Measurement Type) — diagnósticos condicionais | SP-OBS-17JUL 18jul (a4a947e, 3f93606, b23c56f) | medidos, NÃO incluídos — 2E: contagem inicial de 23 casos reais estava inflada por siglas MANUT já presentes no catálogo primário (achado em revisão); corrigida a exclusão, contagem real de siglas MANUT-exclusivas = 0/24 (branch <5, arquivado); 2G: vocabulário PT real do engine (12 valores) já 100% coberto por `_MEASUREMENT_TYPE_PT_EN`, incl. KMDF (resolvido em SP-Pendencias-09jul anterior) |
| 2F — coluna EQUIPAMENTO dedicada (ramo independente I2) | SP-OBS-17JUL 18jul (4d48cb5, 995dc93) | implementado — `analise_colunas._col_equipamento` (mesmo mecanismo de detecção por rótulo de endereço/módulo); `estruturador` preenche `nome_equipamento` só quando vazio (nunca sobrescreve N0), varredura de linha inteira continua rodando (coexistência); **bug achado em revisão e corrigido**: coluna EQUIPAMENTO reentrava na própria varredura de linha e conflitava consigo mesma quando a célula não era forma canônica pura (ex. "52-1 (novo)") — excluída da varredura, igual `c_modulo` já era; conflito genuíno entre fontes continua detectado |
| A1 — sufixo de família (`_DJ`/`_SEC`/`_TR`) no Device Mapping (discreto não-proteção + analógico) | SP-AJUSTES-20JUL 21jul (ddc8842, 439fb3d, d730068) | implementado — `_SUFIXO_FAMILIA` (whitelist `familia_do_id`: Disjuntor/Seccionadora/Transformador); `_device_mapping` anexa o sufixo quando a base termina no `equipamento` resolvido; `_device_mapping_analog` idem no ramo disjuntor (`<DISJ>_DJ`); `dm_registro()` novo — derivação ÚNICA de `(Signal Name, Device Mapping)`, consumida por `_valores`, Task 6, Task 9 (UI) e Task 11; sem equipamento, fallback módulo-duplicado emerge sem sufixo (inalterado); Custom ID intocado nesta task (`particionar_custom_id_duplicado` seguia com `nome_hierarquico` puro — mudou só na Task 11, ver abaixo). Evidência real: FWB (`docs/input_nao_homogeneo_2_FWB.xlsx`) gera 11 DMs com sufixo `_TR` real (módulos TR1/TR2) no fechamento de closeout |
| A2 — disjuntor no ramo PROT de alimentador (supersede 16/07) | SP-AJUSTES-20JUL 21jul (464867e) | implementado — `_device_mapping` ramo `dm_prot`: quando `_eh_alimentador(modulo_nome)` E disjuntor conhecido, o 2º nível do DM vira o disjuntor do módulo (`SUB_AL11_52-1_PROT_SIGLA`) em vez do módulo repetido; sem disjuntor ou módulo não-alimentador mantém o fallback módulo-duplicado (comportamento pré-20/07); decisão do usuário 20/07 SUPERSEDE a correção 16/07 ("proteção sempre módulo-duplicado") só para este caso específico; `_disjuntor_por_modulo`→`disjuntor_por_modulo` (renomeado público, consumido por Task 6/9/11). GTD (fullbase/gate real) não tem combinação alimentador-PROT-disjuntor-conhecido no seu dado — 0 disparos observados no A/B do closeout; FWB idem (0 disparos, apesar de ter módulos AL11-AL25) — evidência de correção fica na escala fullbase (237k+ linhas, Task 4) e nos testes unitários |
| A3 — sufixo `_DJ` no Device Mapping analógico | SP-AJUSTES-20JUL 21jul (d730068) | implementado — `_device_mapping_analog`: ramo disjuntor (grandezas que não são corrente/potência→TC nem tensão→TP) agora emite `<DISJ>_DJ` em vez de `<DISJ>` puro |
| B1 — complemento `dm_prot` por sigla (fullbase, ≥90% PROT, n≥20) | SP-AJUSTES-20JUL 21jul (4f3f025/aac58a5) | implementado — `COMPLEMENTO_DM_PROT` em `defaults.py` (script auditável `scripts/derivar_complemento_dm_prot.py`): `21, 25IE, 27, 27E1, 27E2, 51N, 59, 67, 81, 87, SGF` (estatístico) + `2649` (decisão do usuário 20/07 — dado limpo mapeia PROT mas fica em 76% na fullbase, abaixo do corte 90%, adicionado à mão). Família `79` (ReclosingEnabled) fica FORA de `dm_prot` por design — é função ANSI mas não dispositivo PROT no DM real; famílias vetadas pela spec (`43*, 86, 63*, 71*, 81U*`) confirmadas ausentes da saída do script. `_dm_prot(sigla, sp)`: `sp.signal_type=="RelayTrip"` OR sigla no complemento |
| B2 — aviso 43LR sem 43TC (falta sinal Local) | SP-AJUSTES-20JUL 21jul (677f84b) | implementado — `dispositivos_43lr_sem_43tc(registros)`, advisory-only (não filtra/reordena nada); wired em `pipeline.gerar_tdt` (evento AUDITORIA) e `tela_geracao._montar_avisos` (box de aviso da UI) |
| B3 — gate `particionar_tipo_duplicado` (Signal Type duplicado no mesmo dispositivo) | SP-AJUSTES-20JUL 21jul (77dd619, **corrigido em a7a2655**) | implementado — agrupa por `(dm_registro final, signal_type)`; sinais `Custom` e sinais que caem no ramo PROT (`_dm_prot`) ficam ISENTOS do gate (decisão do usuário: "se cair dentro de um dispositivo de proteção, você pode ter o repetido" — fullbase tem 16 Enabled legítimos no mesmo PROT); grupo colidido inteiro vai para revisão (`tipo_duplicado_dispositivo`), nunca sai calado. **Fix pós-review (a7a2655)**: 1ª versão wireada em `pipeline.executar` (além de `gerar_tdt`) causou 100% falso-positivo em dado real SAN2 (29/156 decididos indevidamente para revisão) — causa dupla: (1) sheets não-homogêneas com coluna SIGLA dedicada nunca reclassificam `tipo_sinal.categoria` para "Analog" via heurística estrutural, então IA/IB/IC ficavam "Discrete" e colidiam por `signal_type="Valor Medido"` (genérico, 55/62 siglas Analógicas compartilham) — corrigido usando `sp.categoria` (do catálogo, confiável) em vez de `rec.tipo_sinal.categoria`, restrito a `categoria=="Discrete"`; (2) par 43LR/43TC (sinais distintos, `signal_type="Local"` genérico na lista v2) colidia como se fossem duplicata — corrigido forçando `43LR` para `Custom` no agrupamento (`_FORCA_CUSTOM_TIPO_DUPLICADO`), robusto contra catálogo desatualizado. Gate agora WIRED em `gerar_tdt` E `executar` (fecha o gap do Concern 1 do report original); `test_integracao_san2.py` confirma 5/5 passam limpo pós-fix |
| C — par de índices distintos vira `MultiCoord`, não `DoubleBit` | SP-AJUSTES-20JUL 21jul (14b41cc) | implementado — `estruturador.estruturar`/`estruturador_homogeneo.estruturar_homogeneo`: 2 índices distintos agora emitem `datatype="MultiCoord"` (era `"DoubleBit"`); `DoubleBit` fica reservado ao ponto nativo de 1 endereço (nenhuma lista de origem o produz hoje) — domínio (`contracts.py`) preserva o valor, só os 2 produtores mudaram. Consumer mapping confirmou nenhum consumidor condicional em `"DoubleBit"` fora dos 2 estruturadores. Evidência real: FWB gera 15 registros `MultiCoord` no fechamento de closeout (era `DoubleBit` antes desta task) |
| D2 — fix falso-positivo do aviso de endereço duplicado (UI) | SP-AJUSTES-20JUL 21jul (99590b1) | implementado — `enderecos_duplicados` (tela_geracao.py) passou a espelhar a MESMA chave do gate do engine (`particionar_endereco_duplicado`): módulo entra na chave (índice local reusado entre módulos DISTINTOS não é duplicata) e registros `Output` contribuem seus índices no espaço "out" (endereço de escrita), não mais colidindo com Inputs reais de mesmo índice — UI nunca reimplementa, só espelha |
| D1 — colunas derivadas Signal Name/Device Mapping/Signal Type na tabela de revisão | SP-AJUSTES-20JUL 21jul (56c4181) | implementado — `modelo_tabela.COLUNAS` renomeia `Sinal→Sigla`, `Descr. ADMS→Descr. lista padrão`, remove `Tokens`/`Descr. normalizada`; 3 colunas novas READ-ONLY (`Signal Name`, `Device Mapping`, `Signal Type`) computadas ao vivo via `dm_registro`/`disjuntor_por_modulo` (não persistidas em `SignalRecord`) — UI espelha o engine, nunca reimplementa a derivação |
| Task 11 (fora das 10 originais, aberta ao vivo 20/07 tarde) — Signal Name usa disjuntor no PROT de alimentador; Custom ID acompanha | SP-AJUSTES-20JUL 21jul (301f2bc) | implementado — origem: usuário reportou pós-Task 8 que o Device Mapping do PROT-alimentador (A2) já usava o disjuntor mas o Signal Name continuava `MOD_MOD` (fallback módulo-duplicado antigo). Fullbase confirma que produção real usa o disjuntor nos TRÊS campos (Signal Name `CNC_AL11_52-22_51F`, Device Mapping `CNC_AL11_52-22_PROT_51F`, Remote Point Custom ID `CNCAL11522251F_UTR_CNC_DNP3_1`) — o Custom ID real deriva do Signal Name CORRIGIDO, não do módulo-duplicado. Decisão do usuário: corrigir Signal Name E Custom ID juntos (supersede a leitura literal do Global Constraint original do plano "nenhuma task pode alterar Custom ID", só para PROT-alimentador-com-disjuntor-conhecido-e-sem-equipamento-explícito). `dm_registro`: variável local `equipamento` recebe fallback do disjuntor (só quando PROT+alimentador+disjuntor+sem equipamento explícito) ANTES de `nome_hierarquico` — mas o `equipamento=` passado a `_device_mapping` continua sendo o original (branch não-PROT da Task 1 intocado). `particionar_custom_id_duplicado` ganhou parâmetro opcional `lista_padrao=None`: quando `None` (retrocompat, testes existentes/chamadas com 1 argumento), mantém `nome_hierarquico` puro; quando informado (os 2 call sites de produção em `pipeline.py` — `gerar_tdt` e `executar`), usa `dm_registro` para casar exatamente o que sai no xlsx. **Invariante final do Custom ID (revisado vs. o texto original do plano)**: NÃO é mais "idêntico a antes" incondicionalmente — é idêntico a antes EXCETO para o caso PROT-alimentador-disjuntor-conhecido-sem-equipamento-explícito, onde intencionalmente segue o disjuntor (evidência fullbase, aprovado pelo usuário) |

## Verification obrigatória de closeout — SP-AJUSTES-20JUL (21jul)

Suíte completa: `python -m pytest tests/ -q` → **1136 passed, 2 xfailed**, 0 failures.

`python -m bench.regressao` (GTD real, `pipeline.executar`): `comum=952 iguais=688 pct=72.3%`, 6 FAIL — todos pré-existentes em `casos_travados.csv` (baseline `2026-07-01`, bugs de classificação não relacionados a este plano: Mola/MLCC, verbo LIGAR, seccionadora/43LR genérico, perda de prefixo AJ, truncamento de sigla, perda de dígito). Comparação A/B direta contra o merge-base (`d026929`=`main`, rodado num worktree descartável): **resultado byte-idêntico** — `comum=952 iguais=688 pct=72.3%`, mesmos 6 FAILs com as mesmas notas. Zero regressão confirmada por comparação direta, não só pela leitura do docstring.

Diff campo-a-campo dos 2 TDTs gerados (baseline vs HEAD, GTD): de 1014 linhas, 1013 têm Signal Name IDÊNTICO entre baseline e HEAD; dessas 1013, ZERO diferem em Device Mapping/Remote Point Custom ID/Input Data Type/Output Data Type. 1 linha mudou de módulo (`GTD_TR2_TR2_FCOM`→`GTD_TR1_TR1_FCOM`) — reatribuição de módulo/classificação, não campo tocado por este plano. Correção (revisão final de branch): Task 7 TOCA `estruturador.py`/`estruturador_homogeneo.py`, mas só troca o rótulo de datatype (`DoubleBit`→`MultiCoord`); nenhum consumidor (`dc_pairer`, `corrigir`, `fundir_pares_posicao`, atribuição de módulo) ramifica nesse valor — a mudança é inerte a module assignment/pareamento, verificado consumidor por consumidor. Conclusão: as mudanças deste plano (sufixo de família, PROT-alimentador-disjuntor, MultiCoord, Signal Name/Custom ID) simplesmente não dispararam no conjunto de sinais específico do GTD — não é evidência de que as features não funcionam (Tasks 1-9/11 têm testes unitários próprios + Task 4/11 têm evidência direta em escala fullbase, 237k+ linhas). **A reatribuição TR1/TR2 fica como pergunta em aberto, fora do escopo deste plano — não investigada nem corrigida aqui.**

Spot-check adicional em FWB (`docs/input_nao_homogeneo_2_FWB.xlsx` vs `docs/TDT/exportTDT_UTR_FWB_1_20260626.xlsx`, gerado via script descartável fora de `bench/regressao.py`, mesma lógica de `_gerar_nosso_tdt`): comum=319 iguais=220 pct=69.0% (informativo, não gate — FWB não está em `bench/regressao.py._PARES`). Evidência CONCRETA das features disparando em dado real: **11 Device Mappings com sufixo `_TR`** (família Transformador, A1/A3, módulos TR1/TR2 — ex. `FWB_TR1_TR2_71T`→`FWB_TR1_TR2_TR`) e **15 registros `Input Data Type=MultiCoord`** (Task C, eram `DoubleBit` antes desta task). PROT-alimentador-disjuntor (A2/Task 11) não disparou em FWB (0 ocorrências do padrão `_<52|24|29|89>-N_PROT_` no DM) apesar de FWB ter módulos alimentador reais (AL11-AL25) — mesma conclusão do GTD: o caso específico (PROT + alimentador + disjuntor único conhecido + sem equipamento explícito) não está presente nestes 2 datasets de uma única subestação; evidência de correção continua na escala fullbase (Task 4/11) e nos testes unitários.

## Verification
Specs revisadas pelo usuário antes de implementar.
