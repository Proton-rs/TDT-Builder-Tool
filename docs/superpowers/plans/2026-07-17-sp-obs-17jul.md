# SP-OBS-17JUL Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar a spec `docs/superpowers/specs/2026-07-17-sp-observacoes-17jul-design.md` — P1 endereços por direção na UI, P2 fontes extras da lista padrão (2A–2G), P3/P3b reparear em lote + separar par de posição, P4 config delta-only, P5 diagnóstico de âncora + correções C1/C2/C4.

**Architecture:** Plano faseado por risco (padrão SP-Unificado): Fase 1 = determinísticos sem scoring (P4, P1); Fase 2 = UI/estado (P3, P3b); Fase 3 = fontes LP de baixo risco (2A, 2D, 2C-uso1); Fase 4 = P5 (diagnóstico obrigatório → correções condicionais, uma por rodada de gate); Fase 5 = fontes LP de médio risco (2B, 2C-uso2, 2E, 2G, 2F). Toda mudança que toca matching/roteamento roda o gate individualmente.

**Tech Stack:** Python 3.12, openpyxl, pytest (+pytest-qt p/ UI), PySide6, tomllib/tomli_w. Sem dependências novas.

## Global Constraints

- **Gate obrigatório individual:** `PYTHONPATH=src python bench/gate_tdt_real.py`; `pct >= baseline` da tarefa anterior. Regrediu → reverter a tarefa, registrar em `bench/resultados/spOBS_<task>.txt`, seguir. NUNCA duas mudanças de scoring na mesma rodada.
- **Suíte:** `PYTHONPATH=src python -m pytest` (1026+ passed no início; nunca fechar task com falha).
- **Fluxo de dados (CLAUDE.md):** edição do usuário nunca revertida; identidade nunca destruída; `scripts/relatorio_fluxo_real.py` = 0 PERDAS no closeout.
- **Decisões da spec §9 (aprovadas 17/07, opção recomendada):** (1) renomear coluna "Endereço"→"Endereço Input"; (2) limpar as 3 linhas de peso do `config.toml` no mesmo commit; (3) P5-C1 decide a variante-pai exata quando discriminadores inconclusivos; (4) em divergência de estados, coluna FUNÇÃO da aba prevalece sobre catálogo MM (com aviso); (5) ordem do §8 mantida; (6) P3b separa por RECONSTRUÇÃO (preservar dado absorvido na fusão = follow-up, fora deste plano).
- Estilo: nomes descritivos, funções puras, teste por função pública. Commits pequenos `fix|feat|test|refactor(escopo)`.
- Ledger `docs/AGENTS.md`: 1 linha por task fechada, no closeout de cada fase.
- Lista padrão default: `docs/Pontos Padrao ADMS_v8.xlsx` (a única com sheet `DE->PARA` garantida — 2A tolera ausência).

---

## Fase 0 — Baseline

### Task 0: Congelar baseline do gate

**Files:**
- Create: `bench/resultados/spOBS_baseline.txt`

- [ ] **Step 1:** Run `PYTHONPATH=src python bench/gate_tdt_real.py` — anotar `pct`.
- [ ] **Step 2:** Escrever no arquivo: `baseline spOBS — 2026-07-17`, `pct=<valor>`, `commit=<git rev-parse --short HEAD>`.
- [ ] **Step 3:** `git add bench/resultados/spOBS_baseline.txt && git commit -m "test(gate): congela baseline spOBS"`

---

## Fase 1 — Determinísticos (P4 config, P1 endereços UI)

### Task 1: P4 — config_io delta-only + migração do trio stale

**Files:**
- Modify: `src/tdt/ui/config_io.py:75-104`
- Modify: `config.toml` (raiz — remover 3 linhas de peso)
- Test: `tests/test_config_io.py`

**Interfaces:**
- Consumes: `Config` (`tdt/config.py`), `_ESCALARES` (`config_io.py:27-32`).
- Produces: `salvar_config(path, config, paths)` grava APENAS campos ≠ `Config()` default; `carregar_config(path)` descarta o trio stale `0.34/0.33/0.33` (só quando os 3 batem juntos). Assinaturas inalteradas.

- [ ] **Step 1: Testes que falham** (`tests/test_config_io.py`):

```python
from tdt.config import Config
from tdt.ui.config_io import carregar_config, salvar_config


def test_salvar_grava_so_deltas(tmp_path):
    p = tmp_path / "config.toml"
    salvar_config(p, Config(), {"input": "", "output": "", "template": "", "lista_padrao": ""})
    texto = p.read_text(encoding="utf-8")
    assert "peso_tfidf" not in texto          # default puro -> nada de [config]
    assert "pesos_regras" not in texto

def test_salvar_grava_override(tmp_path):
    from dataclasses import replace
    p = tmp_path / "config.toml"
    salvar_config(p, replace(Config(), threshold_pct=0.5), {"input": "", "output": "", "template": "", "lista_padrao": ""})
    texto = p.read_text(encoding="utf-8")
    assert "threshold_pct = 0.5" in texto
    assert "peso_tfidf" not in texto

def test_carregar_descarta_trio_stale(tmp_path):
    p = tmp_path / "config.toml"
    p.write_text(
        "[config]\npeso_tfidf = 0.34\npeso_vetorial = 0.33\npeso_fuzzy = 0.33\n"
        "threshold_pct = 0.5\n", encoding="utf-8")
    cfg, _ = carregar_config(p)
    assert cfg.peso_tfidf == 0.70 and cfg.peso_vetorial == 0.25 and cfg.peso_fuzzy == 0.05
    assert cfg.threshold_pct == 0.5           # override legítimo preservado

def test_carregar_preserva_peso_intencional(tmp_path):
    p = tmp_path / "config.toml"
    p.write_text("[config]\npeso_tfidf = 0.34\n", encoding="utf-8")  # só 1 dos 3 -> não é o trio
    cfg, _ = carregar_config(p)
    assert cfg.peso_tfidf == 0.34
```

- [ ] **Step 2:** Run `PYTHONPATH=src python -m pytest tests/test_config_io.py -v` → FAIL.
- [ ] **Step 3: Implementar.** Em `salvar_config` (`config_io.py:94-104`): montar `doc` só com divergentes —

```python
def salvar_config(path: str | Path, config: Config, paths: dict) -> None:
    base = _base_paths()
    paths_relativos = {
        k: _para_relativo(v, base) for k, v in {**_PATHS_VAZIO, **paths}.items()
    }
    default = Config()
    escalares = {
        k: getattr(config, k) for k in _ESCALARES
        if getattr(config, k) != getattr(default, k)
    }
    pesos = {
        k: v for k, v in config.pesos_regras.items()
        if default.pesos_regras.get(k) != v
    }
    doc: dict = {"paths": paths_relativos}
    if escalares:
        doc["config"] = escalares
    if pesos:
        doc["pesos_regras"] = pesos
    Path(path).write_text(tomli_w.dumps(doc), encoding="utf-8")
```

Em `carregar_config` (`config_io.py:86` — logo após montar `knobs`), migração one-time:

```python
_PESOS_STALE = {"peso_tfidf": 0.34, "peso_vetorial": 0.33, "peso_fuzzy": 0.33}
...
    knobs = {k: v for k, v in dados.get("config", {}).items() if k in _ESCALARES}
    if all(knobs.get(k) == v for k, v in _PESOS_STALE.items()):
        # snapshot pré-calibração BM25 (03/07) — descartar o trio, defaults valem
        for k in _PESOS_STALE:
            knobs.pop(k)
```

- [ ] **Step 4:** Run testes → PASS. Rodar também `PYTHONPATH=src python -m pytest tests/ -k config -v` (regressão dos testes existentes de config_io/paths relativos).
- [ ] **Step 5:** Editar `config.toml` da raiz: remover as linhas `peso_tfidf = 0.34`, `peso_vetorial = 0.33`, `peso_fuzzy = 0.33` e a seção `[pesos_regras]` inteira (idêntica ao default — o delta-only não a regravaria).
- [ ] **Step 6:** Commit: `git add src/tdt/ui/config_io.py tests/test_config_io.py config.toml && git commit -m "fix(config): persiste so deltas vs default + migra trio de pesos stale (P4)"`

### Task 2: P4 — tela_config: marcador de override + guard soma dos pesos

**Files:**
- Modify: `src/tdt/ui/tela_config.py` (grupo pesos ~122-140; `recarregar` 225-240; `aplicar` 263-275; `_restaurar_padroes`)
- Test: `tests/test_ui_tela_config.py` (criar se não existir; usar `qtbot` como `tests/test_ui_modelo_tabela.py`)

**Interfaces:**
- Consumes: `Config()` defaults; widgets existentes `spin_pct/gap/topn/tfidf/vet/fuzzy`, `combo_modelo`, `spin_k`; botão "Restaurar padrões" já wired a `_restaurar_padroes` (`tela_config.py:166-167`).
- Produces: label `lbl_overrides` (lista knobs ≠ default); `aplicar()` bloqueia soma de pesos ≠ 1.0.

- [ ] **Step 1:** Ler `_restaurar_padroes` atual (grep `def _restaurar_padroes` em tela_config.py) — confirmar que reseta TODOS os 8 knobs p/ `Config()`; se cobrir só parte, completar no Step 3.
- [ ] **Step 2: Testes que falham**:

```python
from tdt.config import Config
from tdt.ui.estado import AppState
from tdt.ui.tela_config import TelaConfig


def test_overrides_visiveis(qtbot, tmp_path):
    from dataclasses import replace
    st = AppState(config=replace(Config(), peso_tfidf=0.34))
    tela = TelaConfig(st, config_path=tmp_path / "c.toml")
    qtbot.addWidget(tela)
    assert "peso_tfidf" in tela.lbl_overrides.text()

def test_sem_override_label_vazia(qtbot, tmp_path):
    tela = TelaConfig(AppState(), config_path=tmp_path / "c.toml")
    qtbot.addWidget(tela)
    assert not tela.lbl_overrides.isVisible() or tela.lbl_overrides.text() == ""

def test_aplicar_bloqueia_soma_invalida(qtbot, tmp_path):
    st = AppState()
    tela = TelaConfig(st, config_path=tmp_path / "c.toml")
    qtbot.addWidget(tela)
    tela.spin_tfidf.setValue(0.9); tela.spin_vet.setValue(0.9); tela.spin_fuzzy.setValue(0.9)
    tela.aplicar()
    assert st.config.peso_tfidf == Config().peso_tfidf   # não aplicou
    assert not (tmp_path / "c.toml").exists()
```

- [ ] **Step 3: Implementar.** No `__init__` (após o grupo de pesos, ~linha 140): `self.lbl_overrides = QLabel(""); self.lbl_overrides.setProperty("nivel", "aviso"); layout.addWidget(self.lbl_overrides)`. Novo método (conectado a `valueChanged` dos 6 spins + chamado no fim de `recarregar`):

```python
_KNOBS_UI = (
    ("threshold_pct", "spin_pct"), ("threshold_gap", "spin_gap"),
    ("top_n_pct", "spin_topn"), ("peso_tfidf", "spin_tfidf"),
    ("peso_vetorial", "spin_vet"), ("peso_fuzzy", "spin_fuzzy"),
)

def _atualizar_overrides(self, *_args) -> None:
    default = Config()
    difs = [
        f"{campo} ({getattr(self, spin).value():g} ≠ padrão {getattr(default, campo):g})"
        for campo, spin in _KNOBS_UI
        if abs(getattr(self, spin).value() - getattr(default, campo)) > 1e-9
    ]
    self.lbl_overrides.setText(
        "Valores diferentes do padrão calibrado: " + "; ".join(difs) if difs else "")
    self.lbl_overrides.setVisible(bool(difs))
```

No `aplicar()` (topo, antes do `replace`): guard —

```python
    soma = self.spin_tfidf.value() + self.spin_vet.value() + self.spin_fuzzy.value()
    if abs(soma - 1.0) > 0.001:
        QMessageBox.warning(self, "Pesos do ensemble",
                            f"Os pesos somam {soma:.3f} — ajuste para 1.0 antes de salvar.")
        return
```

- [ ] **Step 4:** Run `PYTHONPATH=src python -m pytest tests/test_ui_tela_config.py -v` → PASS.
- [ ] **Step 5:** Commit: `git commit -am "feat(ui): marcador de override + guard soma de pesos na tela config (P4)"`

### Task 3: P1 — endereços por direção na UI (display + edição + rename da coluna)

**Files:**
- Modify: `src/tdt/ui/modelo_tabela.py` (COLUNAS:14-21; `_texto`:221-224; `setData`:315-364; `_EDITAVEIS`)
- Modify: consumidores do nome "Endereço" (Step 1 localiza)
- Test: `tests/test_ui_modelo_tabela.py`

**Interfaces:**
- Produces: função módulo-level `enderecos_exibicao(rec) -> tuple[str, str]` (célula Input, célula Output); coluna renomeada `"Endereço Input"`.
- Regra (tabela da spec §3): direção `Output` → Input="—", Output=`indices`; demais → Input=`indices`, Output=`indices_saida` ou "—".

- [ ] **Step 1:** `grep -rn '"Endereço"' src tests` — listar todos os pontos com o nome antigo (COLUNAS, `_texto`, `setData`, `_EDITAVEIS`, testes, filtros/telas se houver). Todos mudam neste task.
- [ ] **Step 2: Testes que falham** (usar helpers `_rec`/`_state`/`_col` existentes do arquivo):

```python
def _rec_output():
    return replace(_rec(), tipo_sinal=TipoSinal("Discrete", "SingleBit", "Output"),
                   enderecamento=Enderecamento("DNP3", (700,)))

def test_output_mostra_endereco_na_coluna_output(qtbot):
    m = ModeloSinais(_state(_rec_output()))
    assert m.valor_texto(0, _col("Endereço Input")) == "—"
    assert m.valor_texto(0, _col("Endereço Output")) == "700"

def test_input_mantem_colunas_atuais(qtbot):
    m = ModeloSinais(_state(_rec()))          # _rec: Input, indices=(1,)
    assert m.valor_texto(0, _col("Endereço Input")) == "1"
    assert m.valor_texto(0, _col("Endereço Output")) == "—"

def test_editar_endereco_output_de_output_escreve_em_indices(qtbot):
    st = _state(_rec_output())
    m = ModeloSinais(st)
    assert m.setData(m.index(0, _col("Endereço Output")), "710", Qt.EditRole)
    assert st.registros[0].enderecamento.indices == (710,)
    assert st.registros[0].enderecamento.indices_saida == ()

def test_editar_endereco_input_de_output_rejeitado(qtbot):
    m = ModeloSinais(_state(_rec_output()))
    assert m.setData(m.index(0, _col("Endereço Input")), "9", Qt.EditRole) is False
```

- [ ] **Step 3: Implementar.** Renomear em `COLUNAS`: `"Endereço"` → `"Endereço Input"`. Função nova + branches:

```python
def enderecos_exibicao(rec) -> tuple[str, str]:
    """(célula Endereço Input, célula Endereço Output) conforme a direção —
    espelha engine_tdt._valores: Output puro tem endereço PRÓPRIO de escrita."""
    ins = ";".join(str(i) for i in rec.enderecamento.indices)
    outs = ";".join(str(i) for i in rec.enderecamento.indices_saida)
    if rec.tipo_sinal.direcao == "Output":
        return "—", ins or "—"
    return ins, outs or "—"
```

Em `_texto` (substitui as linhas 221-224):

```python
        if nome == "Endereço Input":
            return enderecos_exibicao(rec)[0]
        if nome == "Endereço Output":
            return enderecos_exibicao(rec)[1]
```

Em `setData` (substitui o branch de endereço, mantendo a validação 0..65535):

```python
        elif nome in ("Endereço Input", "Endereço Output"):
            try:
                indices = tuple(int(p) for p in texto.split(";")) if texto else ()
            except ValueError:
                return False
            if not all(0 <= v <= 65535 for v in indices):
                return False
            rec = self._estado.registros[linha]
            if rec.tipo_sinal.direcao == "Output":
                if nome == "Endereço Input":
                    return False        # célula "—": Output não tem endereço de leitura
                campo = "indices"       # endereço próprio (de escrita) do comando
            else:
                campo = "indices" if nome == "Endereço Input" else "indices_saida"
            self._estado.definir_enderecos(linha, campo, indices)
```

Atualizar `_EDITAVEIS` e todos os pontos do Step 1 (inclusive testes que usam `_col("Endereço")`).

- [ ] **Step 4:** Run `PYTHONPATH=src python -m pytest tests/test_ui_modelo_tabela.py -v` → PASS (novos + antigos).
- [ ] **Step 5:** Suíte UI inteira: `PYTHONPATH=src python -m pytest tests/ -k ui -v` → PASS. TDT não muda (é só UI) — sem gate.
- [ ] **Step 6:** Commit: `git commit -am "fix(ui): enderecos exibidos/editados por direcao; renomeia coluna p/ Endereco Input (P1)"`

---

## Fase 2 — Revisão: P3 reparear em lote + P3b separar par de posição

### Task 4: P3 — núcleo puro `reparear`

**Files:**
- Create: `src/tdt/ui/reparear.py`
- Test: `tests/test_reparear.py`

**Interfaces:**
- Consumes: `dc_pairer.parear(registros, config) -> (tuple[SignalRecord], tuple[ItemRevisao])`; `normalizador_estrutural.fundir_pares_posicao(registros, whitelist_posicao)`.
- Produces:

```python
@dataclass(frozen=True)
class ResultadoReparear:
    resultantes: tuple[SignalRecord, ...]   # substituem os elegíveis (fundidos + sós + ambíguos re-marcados)
    n_fundidos: int
    n_ambiguos: int
    n_sem_par: int

def elegivel(rec: SignalRecord) -> bool
def reparear(elegiveis: list[SignalRecord], whitelist_posicao: frozenset[str], config: Config) -> ResultadoReparear
```

- [ ] **Step 0:** Ler o branch `"Motivo"` de `modelo_tabela._texto` (~linhas 200-230) p/ confirmar de onde a coluna Motivo lê (o código abaixo assume `rec.justificativa` carregando a chave do motivo, padrão `pipeline.py` "estado_sem_candidato"). Ajustar o campo usado se a fonte for outra. Também: `grep -n "fundir_pares_posicao" src/tdt/pipeline.py` e copiar a EXPRESSÃO exata da whitelist usada nos 2 call sites — a UI passa a mesma.
- [ ] **Step 1: Testes que falham** (helpers `_rec` no estilo `tests/test_ui_estado.py`; Input com sigla `SECF`, Output com mesma sigla/módulo/equipamento):

```python
def test_reparear_funde_par_reclassificado():
    status = _rec("s:1", "SECF", direcao="Input", indices=(100,))
    comando = _rec("c:1", "SECF", direcao="Output", indices=(400,))
    res = reparear([status, comando], frozenset(), Config())
    assert res.n_fundidos == 1
    fundido = [r for r in res.resultantes if r.tipo_sinal.direcao == "InputOutput"][0]
    assert fundido.enderecamento.indices == (100,)
    assert fundido.enderecamento.indices_saida == (400,)

def test_reparear_nao_toca_inputoutput_existente():
    ja_fundido = _rec("f:1", "SECF", direcao="InputOutput", indices=(1,), saida=(2,))
    assert elegivel(ja_fundido) is False

def test_reparear_nxm_vira_ambiguo_sem_perder_registro():
    recs = [_rec(f"s:{i}", "SECF", direcao="Input", indices=(i,)) for i in (1, 2)]
    recs += [_rec("c:9", "SECF", direcao="Output", indices=(9,))]
    res = reparear(recs, frozenset(), Config())
    total_indices = sum(len(r.enderecamento.indices) + len(r.enderecamento.indices_saida)
                        for r in res.resultantes)
    assert total_indices == 3                      # conservação de endereços
```

- [ ] **Step 2:** Run `PYTHONPATH=src python -m pytest tests/test_reparear.py -v` → FAIL (módulo não existe).
- [ ] **Step 3: Implementar** `src/tdt/ui/reparear.py`:

```python
"""Repareamento em lote da revisão (SP-OBS-17JUL P3): reusa o pipeline puro
(fundir_pares_posicao + dc_pairer.parear) sobre um subconjunto elegível.
NUNCA desfaz par existente nem edição do usuário (CLAUDE.md fluxo de dados)."""
from dataclasses import dataclass, replace

from tdt.config import Config
from tdt.contracts import SignalRecord
from tdt.dc_pairer import parear
from tdt.normalizador_estrutural import fundir_pares_posicao


@dataclass(frozen=True)
class ResultadoReparear:
    resultantes: tuple[SignalRecord, ...]
    n_fundidos: int
    n_ambiguos: int
    n_sem_par: int


def elegivel(rec: SignalRecord) -> bool:
    if not rec.sigla_sinal:
        return False
    d = rec.tipo_sinal.direcao
    if d == "Input":
        return not rec.enderecamento.indices_saida
    return d == "Output"


def reparear(elegiveis, whitelist_posicao, config: Config) -> ResultadoReparear:
    pos = fundir_pares_posicao(list(elegiveis), whitelist_posicao)
    decididos, revisao = parear(pos, config)
    ambiguos = tuple(
        replace(ir.registro, status="revisao", justificativa=ir.motivo)
        for ir in revisao
    )
    n_fundidos = sum(1 for r in decididos if r.tipo_sinal.direcao == "InputOutput")
    n_sem_par = len(decididos) - n_fundidos
    return ResultadoReparear(
        resultantes=tuple(decididos) + ambiguos,
        n_fundidos=n_fundidos, n_ambiguos=len(ambiguos), n_sem_par=n_sem_par,
    )
```

- [ ] **Step 4:** Run testes → PASS.
- [ ] **Step 5:** Commit: `git commit -am "feat(ui): nucleo puro de repareamento em lote (P3)"`

### Task 5: P3 — AppState.aplicar_reparear + botão "Reparear sheet"

**Files:**
- Modify: `src/tdt/ui/estado.py` (novo método, após `formar_par_posicao`)
- Modify: `src/tdt/ui/tela_revisao.py` (botão na toolbar :197-218; callback)
- Test: `tests/test_ui_estado.py`, `tests/test_ui_tela_revisao.py` (se existir; senão smoke no test_ui_estado)

**Interfaces:**
- Produces: `AppState.aplicar_reparear(ids: list[str], whitelist_posicao, config) -> ResultadoReparear` — 1 snapshot, ordem preservada, absorvidos removidos.

- [ ] **Step 1: Teste que falha**:

```python
def test_aplicar_reparear_preserva_ordem_e_um_undo():
    st = AppState()
    st.registros = [
        _rec("x:0", None, "revisao"),                       # não elegível (sem sigla)
        _rec_dir("s:1", "SECF", "Input", (100,)),
        _rec_dir("c:1", "SECF", "Output", (400,)),
    ]
    res = st.aplicar_reparear([r.id for r in st.registros], frozenset(), Config())
    assert res.n_fundidos == 1
    assert st.registros[0].id == "x:0"                      # intocado, na posição
    assert len(st.registros) == 2                           # comando absorvido
    assert st.desfazer() and len(st.registros) == 3         # 1 undo restaura tudo
```

- [ ] **Step 2:** FAIL. **Step 3: Implementar** em `estado.py`:

```python
    def aplicar_reparear(self, ids, whitelist_posicao, config) -> "ResultadoReparear":
        """Reparea os registros elegíveis dentre `ids` (P3). 1 snapshot: 1
        desfazer reverte o lote. Ordem preservada; comando absorvido pela
        fusão sai da lista (endereço vive no par — conservação auditada)."""
        from tdt.ui.reparear import ResultadoReparear, elegivel, reparear
        alvo = set(ids)
        elegiveis = [r for r in self.registros if r.id in alvo and elegivel(r)]
        if not elegiveis:
            return ResultadoReparear((), 0, 0, 0)
        self._snapshot()
        res = reparear(elegiveis, whitelist_posicao, config)
        ids_eleg = {r.id for r in elegiveis}
        novos_por_id = {r.id: r for r in res.resultantes}
        saida = []
        for r in self.registros:
            if r.id in ids_eleg:
                novo = novos_por_id.get(r.id)
                if novo is not None:
                    saida.append(novo)
            else:
                saida.append(r)
        self.registros = saida
        return res
```

- [ ] **Step 4:** PASS. **Step 5: UI** (`tela_revisao.py`): botão na toolbar ao lado de "Parear D+C" (`btn_reparear = QPushButton("Reparear sheet")`, wired a `_reparear_sheet`); callback:

```python
    def _reparear_sheet(self):
        indice_aba = self.abas_sheet.currentIndex()
        sheet = None if indice_aba <= 0 else self.abas_sheet.tabData(indice_aba)
        selecao = self._linhas_selecionadas()
        if len(selecao) >= 2:
            usar_selecao = QMessageBox.question(
                self, "Reparear", "Reparear só as linhas selecionadas?\n(Não = sheet inteira)",
            ) == QMessageBox.Yes
        else:
            usar_selecao = False
        if usar_selecao:
            ids = [self._estado.registros[i].id for i in selecao]
        else:
            ids = [r.id for r in self._estado.registros
                   if sheet is None or r.modulo.sheet_name == sheet]
        res = self._estado.aplicar_reparear(ids, _WHITELIST_POSICAO_UI, self._estado.config)
        self.refresh()
        if res.n_fundidos:
            self._selecionar_primeiro_fundido(res)   # observações item 10
        QMessageBox.information(
            self, "Reparear",
            f"{res.n_fundidos} pares formados, {res.n_ambiguos} ambíguos, "
            f"{res.n_sem_par} sem par.")
```

`_WHITELIST_POSICAO_UI` = a MESMA expressão dos call sites do pipeline (Step 0 do Task 4). `_selecionar_primeiro_fundido`: localizar o primeiro id fundido em `self._estado.registros`, mapear pro proxy e `self.tabela.selectRow(...)` + `scrollTo`. Campo do sheet no registro: confirmar `rec.modulo.sheet_name` (fixture `Modulo("M", "sheet_name")` em test_ui_estado:12).
- [ ] **Step 6:** Suíte `-k "ui or reparear"` → PASS. Smoke manual: abrir revisão de lista real (LVA), reclassificar 2 D+C, "Reparear sheet", conferir resumo + undo.
- [ ] **Step 7:** Commit: `git commit -am "feat(ui): botao Reparear sheet na revisao (P3)"`

### Task 6: P3b — `AppState.separar_par_posicao` + botão

**Files:**
- Modify: `src/tdt/ui/estado.py` (após `trocar_sigla_par`; import `uuid`)
- Modify: `src/tdt/ui/tela_revisao.py` (botão + callback, padrão `_formar_par_posicao`:656-672)
- Test: `tests/test_ui_estado.py`

**Interfaces:**
- Produces: `AppState.separar_par_posicao(id_: str) -> str | None` (erro sem mutar | None no sucesso).

- [ ] **Step 1: Testes que falham**:

```python
def test_separar_par_posicao_reconstroi_duas_metades():
    st = AppState()
    st.registros = [_rec("a:1", "DJF1", "decidido")]
    st.registros[0] = replace(
        st.registros[0],
        tipo_sinal=TipoSinal("Discrete", "MultiCoord", "Input"),
        enderecamento=Enderecamento("DNP3", (900, 901)),
    )
    assert st.separar_par_posicao("a:1") is None
    assert len(st.registros) == 2
    a, b = st.registros
    assert a.enderecamento.indices == (900,) and b.enderecamento.indices == (901,)
    assert a.sigla_sinal is None and a.status == "revisao"
    assert a.tipo_sinal.datatype == "SingleBit"
    assert a.id != b.id
    assert st.desfazer() and len(st.registros) == 1

def test_separar_rejeita_nao_multicoord_sem_mutar():
    st = AppState()
    st.registros = [_rec("a:1", "DJF1", "decidido")]     # SingleBit, 1 índice
    assert st.separar_par_posicao("a:1") is not None
    assert len(st._historico) == 0                        # sem snapshot em erro

def test_separar_rejeita_inputoutput():
    st = AppState()
    st.registros = [replace(
        _rec("a:1", "DJF1", "decidido"),
        tipo_sinal=TipoSinal("Discrete", "MultiCoord", "InputOutput"),
        enderecamento=Enderecamento("DNP3", (900, 901), (950,)),
    )]
    assert "desvincule" in st.separar_par_posicao("a:1").lower()
```

- [ ] **Step 2:** FAIL. **Step 3: Implementar** (código da spec §5-P3b):

```python
    def separar_par_posicao(self, id_: str) -> str | None:
        """Inverso de formar_par_posicao (P3b). RECONSTRÓI (a descrição do
        registro absorvido não existe mais — limitação documentada na spec):
        2 Inputs de 1 índice, sigla limpa, revisão `posicao_ambigua`."""
        indice_por_id = {r.id: i for i, r in enumerate(self.registros)}
        i = indice_por_id.get(id_)
        if i is None:
            return "Registro não encontrado."
        r = self.registros[i]
        if r.tipo_sinal.direcao == "InputOutput":
            return "O par tem comando pareado: desvincule (Parear D+C) antes de separar."
        if (r.tipo_sinal.direcao != "Input"
                or r.tipo_sinal.datatype != "MultiCoord"
                or len(r.enderecamento.indices) != 2):
            return "Selecione um par de posição (Input MultiCoord com 2 endereços)."
        if (r.sigla_sinal or "").upper() not in _SIGLAS_POSICAO:
            return "A sigla do registro não é de posição."
        self._snapshot()
        base = replace(
            r,
            tipo_sinal=replace(r.tipo_sinal, datatype="SingleBit"),
            sigla_sinal=None, status="revisao",
            justificativa="posicao_ambigua",   # par separado manualmente — redecidir
        )
        met_a = replace(base, enderecamento=Enderecamento(
            r.enderecamento.protocolo, (r.enderecamento.indices[0],)))
        met_b = replace(base, id=f"{r.id}_pos_{uuid.uuid4().hex[:6]}",
                        enderecamento=Enderecamento(
                            r.enderecamento.protocolo, (r.enderecamento.indices[1],)))
        novos = list(self.registros)
        novos[i] = met_a
        novos.insert(i + 1, met_b)
        self.registros = novos
        return None
```

(Se o Step 0 do Task 4 tiver mostrado que a coluna Motivo NÃO lê de `justificativa`, usar o mecanismo real encontrado lá.)
- [ ] **Step 4:** PASS. **Step 5: UI** — botão `self.btn_separar_par = QPushButton("Separar par de posição")` ao lado do formar (toolbar :197-218), callback:

```python
    def _separar_par_posicao(self):
        indices = self._linhas_selecionadas()
        if len(indices) != 1:
            return
        rec = self._estado.registros[indices[0]]
        ends = ";".join(str(x) for x in rec.enderecamento.indices)
        if QMessageBox.question(
                self, "Separar par de posição",
                f"Separar {rec.sigla_sinal or rec.id} (end. {ends}) em 2 sinais "
                f"de 1 endereço? As metades vão para revisão.") != QMessageBox.Yes:
            return
        erro = self._estado.separar_par_posicao(rec.id)
        if erro is not None:
            QMessageBox.warning(self, "Separar par de posição", erro)
            return
        self.refresh()
```

- [ ] **Step 6:** Suíte UI → PASS. Ciclo manual formar→separar→formar sem colisão de id.
- [ ] **Step 7:** Commit: `git commit -am "feat(ui): separar par de posicao (P3b, reconstrucao)"`

---

## Fase 3 — Lista padrão, baixo risco (2A, 2D, 2C-uso1)

### Task 7: 2A — loader do mapa DE->PARA

**Files:**
- Modify: `src/tdt/dados/lista_padrao.py` (campo novo em `ListaPadraoADMS` + leitura em `carregar`:135-175)
- Test: `tests/test_lista_padrao.py`

**Interfaces:**
- Produces: `ListaPadraoADMS.de_para: dict[str, str]` (sigla entrada UPPER → canônica UPPER; `{}` se a sheet não existir).

- [ ] **Step 1: Teste que falha** (workbook sintético em tmp_path):

```python
def test_carregar_le_sheet_de_para(tmp_path):
    import openpyxl
    wb = openpyxl.Workbook()
    # sheets mínimas que carregar() exige (copiar setup do teste existente de carregar)
    ws = wb.create_sheet("DE->PARA")
    ws.append(["SINAL", "DESCRIÇÃO NOVA"]); ws.append(["90", "R90"]); ws.append(["21_1", "21Z1"])
    p = tmp_path / "lp.xlsx"; wb.save(p)
    lp = ListaPadraoADMS.carregar(p)
    assert lp.de_para == {"90": "R90", "21_1": "21Z1"}

def test_carregar_sem_sheet_de_para(tmp_path):
    # workbook só com as 2 sheets obrigatórias -> de_para == {}
    ...  # mesmo setup mínimo do teste acima, sem a sheet extra
    assert lp.de_para == {}
```

(O "setup mínimo" NÃO é placeholder para o executor: copiar o fixture de workbook do teste de `carregar` já existente em `tests/test_lista_padrao.py` — ele já monta DiscreteSignals/AnalogSignals válidas.)
- [ ] **Step 2:** FAIL. **Step 3: Implementar** — em `carregar`, antes do `return`:

```python
        de_para: dict[str, str] = {}
        if "DE->PARA" in wb.sheetnames:
            for r in wb["DE->PARA"].iter_rows(min_row=2, values_only=True):
                de = str(r[0]).strip().upper() if r and r[0] is not None else ""
                para = (str(r[1]).strip().upper()
                        if r and len(r) > 1 and r[1] is not None else "")
                if de and para:
                    de_para[de] = para
```

e passar ao construtor (campo `de_para: dict[str, str] = field(default_factory=dict)` no dataclass — se `ListaPadraoADMS` for frozen com tuples, seguir o padrão dos campos existentes).
- [ ] **Step 4:** PASS. Rodar `-k lista_padrao` inteiro (retrocompat). **Step 5:** Commit `feat(dados): loader do mapa DE->PARA da lista padrao (2A)`.

### Task 8: 2A — aplicar DE->PARA nos 2 choke points de sigla + gate

**Files:**
- Modify: `src/tdt/normalizacao/estruturador.py:171-176` (branch `tem_sigla`)
- Modify: `src/tdt/normalizacao/estruturador_homogeneo.py` (leitura de `sigla`, ~linha 90)
- Modify: `docs/fluxo_dados.md` (linha do estruturador: registrar a normalização)
- Test: `tests/test_estruturador.py`, `tests/test_estruturador_homogeneo.py`

**Interfaces:**
- Consumes: `lp.de_para` (Task 7). O estruturador heterogêneo recebe `siglas_set` — verificar no call site (pipeline) se `lp` está acessível; senão passar `de_para: dict` como novo param default `{}`.

- [ ] **Step 1: Testes que falham** (nos 2 arquivos; padrão dos testes de pré-classificação existentes):

```python
def test_sigla_de_entrada_normalizada_por_de_para():
    # célula de sigla "90" + de_para {"90": "R90"} + "R90" em siglas_set
    # -> registro sai com sigla_sinal == "R90", decidido
```

(código completo no estilo do teste vizinho de sigla-por-coluna — usar o mesmo fixture de rows; asserção nova: `sigla_sinal == "R90"`.)
- [ ] **Step 2:** FAIL. **Step 3: Implementar.** Heterogêneo (`estruturador.py:172`):

```python
    sv = str(row[c_sigla] or "").strip().upper()
    sv = de_para.get(sv, sv)          # DE->PARA: canoniza sigla de origem (2A)
    if sv and sv in siglas_set:
```

Homogêneo (`estruturador_homogeneo.py`, no ponto onde `sigla` é lida): mesma substituição + aviso na lista `avisos` (4º elemento do retorno): `avisos.append(f"{sheet_name}:{i}: sigla '{bruta}' normalizada p/ '{sigla}' (DE->PARA)")`. A célula original permanece intacta (rastreabilidade).
- [ ] **Step 4:** PASS. **Step 5: Gate** — `pct >= baseline` (Task 0). Regrediu → reverter, registrar `bench/resultados/spOBS_2A.txt`. **Step 6:** atualizar `docs/fluxo_dados.md` (linha `estruturador.estruturar`: "canoniza sigla via DE->PARA (aviso no homogêneo)"). **Step 7:** Commit `feat(analise): canoniza sigla de entrada via DE->PARA (2A)`.

### Task 9: 2D — SEVERIDADE como metadado (loader + coluna na revisão)

**Files:**
- Modify: `src/tdt/dados/lista_padrao.py` (campo `severidade` + coluna no mapping da sheet DiscreteSignals)
- Modify: `src/tdt/ui/modelo_tabela.py` (coluna "Severidade" em COLUNAS + `_texto`)
- Test: `tests/test_lista_padrao.py`, `tests/test_ui_modelo_tabela.py`

- [ ] **Step 1:** Localizar o dict de mapping de colunas em `_ler_sheet(wb["DiscreteSignals"], ...)` (`lista_padrao.py:138-142`) e o helper de lookup por sigla da `ListaPadraoADMS` (`grep -n "def " src/tdt/dados/lista_padrao.py`) — o `_texto` usa esse helper (construir dict local `{sigla: sp}` uma vez no `__init__` do modelo se não houver).
- [ ] **Step 2: Testes que falham:** loader lê `SEVERIDADE` (célula é fórmula — `data_only=True` já resolve, medição 17/07: 690/692 preenchidas); `_texto` da coluna nova devolve `"Severidade 4"` p/ registro com sigla presente na LP e `""` sem sigla/sem LP.
- [ ] **Step 3: Implementar:** campo `severidade: str | None = None` no `SinalPadrao`; entrada `"SEVERIDADE": "severidade"` no mapping; em `modelo_tabela`: `"Severidade"` no fim de COLUNAS (coluna NÃO editável — fora de `_EDITAVEIS`) + branch:

```python
        if nome == "Severidade":
            sp = self._lp_por_sigla.get((rec.sigla_sinal or "").upper())
            return sp.severidade if sp is not None else ""
```

- [ ] **Step 4:** PASS. NÃO toca matching (veto do ledger: severidade nunca em corpus/score) — sem gate. **Step 5:** Commit `feat(ui): coluna Severidade (metadado da LP, nao-scoring) (2D)`.

### Task 10: 2C-uso1 — integridade MM na carga (aviso, não-scoring)

**Files:**
- Modify: `src/tdt/dados/lista_padrao.py` (loader opcional do catálogo + validador)
- Test: `tests/test_lista_padrao.py`

**Interfaces:**
- Produces: `ListaPadraoADMS.catalogo_mm: frozenset[str]` (sheet `Message Mapping`, coluna `Name`; vazio se ausente) e `validar_mm(lp) -> list[str]` (avisos "MM X da sigla Y ausente do catálogo").

- [ ] **Step 1: Teste que falha:** workbook sintético com Message Mapping de 2 nomes; sigla com `mm` fora do catálogo → 1 aviso; com catálogo vazio → 0 avisos (retrocompat).
- [ ] **Step 2:** FAIL. **Step 3:** Implementar (leitura análoga à Task 7; `validar_mm` percorre `discretos+discrete_analog` comparando `sp.mm` contra o catálogo). Wire: `grep -n "avisos" src/tdt/pipeline.py` — anexar os avisos de `validar_mm` ao canal de avisos existente do `executar` (mesma lista onde os avisos do estruturador homogêneo chegam). Hoje essa checagem só existe offline em `tests/fixtures/mm_catalogo_real.txt` — ela continua; isto é o espelho em runtime.
- [ ] **Step 4:** PASS. Sem gate (não toca scoring). **Step 5:** Commit `feat(dados): valida MM da LP contra catalogo Message Mapping na carga (2C-uso1)`.

---

## Fase 4 — P5: diagnóstico + correções condicionais

### Task 11: P5 Fase-0 — `bench/diag_ancora_revisao.py`

**Files:**
- Create: `bench/diag_ancora_revisao.py`
- Create: `docs/superpowers/specs/2026-07-17-diag-ancora-revisao.md` (achados)

- [ ] **Step 1: Escrever o script.** Estrutura (mesmo padrão de invocação dos bench/diag_* existentes — conferir um deles p/ o boilerplate de carga de listas):

```python
"""Diagnóstico P5 (SP-OBS-17JUL): âncora de sigla detectada que terminou em
revisão/sem decisão + generalização do caso 81U1 (partição por classe de MM).
Uso: PYTHONPATH=src python bench/diag_ancora_revisao.py"""
```

Três saídas (imprimir em CSV/tabela):
1. **Casos:** para cada lista real (`docs/input_*.xlsx` + as listas GTA/GTD referenciadas nos bench existentes): rodar o pipeline (mesma chamada `executar(...)`/`diagnostico=True` usada por `bench`/SP-I) e, para cada registro com `ancoragem_sigla.detectar(...)` não-vazio e `status != "decidido"`: lista, id, descrição, âncora(s) + exata/junção, família, top-3 `(sigla, score, fonte)`, gap, `justificativa`. Classificar cada caso: H1 (variantes da mesma família no top), H2 (`score - ajuste < piso`), H3 (âncora fora dos candidatos finais), outra.
2. **Partição LP (generalização 81U1):** agrupar siglas da LP por família (`ancoragem_sigla._familia`); por família, tabela `classe_do_mm(sp.mm)` → variantes; marcar famílias onde alguma classe isola exatamente 1 variante. Esperado: 81 → ATIVACAO isola `81U*`; contra-exemplo 87 (tudo NORMAL@ATUADO).
3. **FP do léxico:** rodar `semantica_estados.detectar_estado` sobre todas as descrições normalizadas das listas; listar tokens que casaram por PREFIXO mas não são a palavra do léxico (padrão do bug "LOCALIZADOR"→LOCAL_REMOTO).

- [ ] **Step 2:** Rodar; escrever os achados com contagens em `docs/superpowers/specs/2026-07-17-diag-ancora-revisao.md`, com veredito explícito por hipótese: `H1 confirmada (N casos)` / `não confirmada`, e a lista de famílias onde C4 discrimina.
- [ ] **Step 3:** Commit `docs(diag): ancora->revisao + particao por classe de MM (P5 fase 0)`.

> **Gate de decisão:** Tasks 12–14 são CONDICIONAIS ao diagnóstico. Hipótese não confirmada → marcar a task como pulada aqui no plano com uma linha citando o número do diagnóstico, registrar no ledger, seguir.

### Task 12 (se H1): C1 — desambiguação de variante ancorada + motivo `variante_ambigua`

**Files:**
- Modify: `src/tdt/ancoragem_sigla.py` (flag `exata` na `Ancora`; função nova `desambiguar_variante`)
- Modify: `src/tdt/pipeline.py:442-468` (região do motivo)
- Modify: `src/tdt/ui/modelo_tabela.py:23-46` (`_MOTIVO_LABEL`)
- Test: `tests/test_ancoragem_sigla.py`, `tests/test_pipeline.py` (ou o arquivo de integração equivalente)

- [ ] **Step 1:** `Ancora` ganha `exata: bool = True`; em `detectar` (`ancoragem_sigla.py:66-98`), a âncora por junção nasce `Ancora(sigla=..., exata=False)`. Teste: detectar em "descrição com 79" → exata True; junção de tokens → False.
- [ ] **Step 2: Teste que falha (C1):**

```python
def test_variante_pai_exata_decide_quando_discriminadores_inconclusivos():
    # âncora exata "79"; candidatos finais 79 (0.85) e 79OK (0.83) -> gap < threshold
    # hoje: revisão score_baixo. Esperado: decide "79" (variante-pai exata, decisão spec §9.3)

def test_sem_variante_pai_vira_variante_ambigua():
    # âncora exata "79" mas candidatos só 79OK/79LO -> revisão com justificativa "variante_ambigua"
```

- [ ] **Step 3: Implementar** `desambiguar_variante(rec, ancoras, config) -> SignalRecord | None` em `ancoragem_sigla.py`: só âncoras `exata` (salvaguarda da spec); se o conjunto de candidatos finais ⊆ família ancorada e existe candidato com `sigla == âncora` → devolver `replace(rec, sigla_sinal=ancora, status="decidido", justificativa="variante-pai exata da âncora (C1)")`; senão None. No `pipeline.py:442-468`, antes de montar `motivo_conf`:

```python
    if d.status == "revisao" and _ancoras and not _multiplas:
        resolvido = ancoragem_sigla.desambiguar_variante(d, _ancoras, config)
        if resolvido is not None and resolvido.status == "decidido":
            return resolvido, None
        if resolvido is not None:
            d = resolvido      # C4 (Task 14) pode devolver revisão com candidatos estreitados
        motivo_conf = "variante_ambigua"
    else:
        motivo_conf = "sigla_multipla" if _multiplas else "score_baixo"
```

`_MOTIVO_LABEL["variante_ambigua"] = "Família decidida pela sigla; escolher variante"`.
- [ ] **Step 4:** PASS. **Step 5: Gate individual** — regrediu → revert + `spOBS_C1.txt`. **Step 6:** Commit `feat(roteador): C1 variante-pai exata + motivo variante_ambigua (P5)`.

### Task 13 (se H2): C2 — piso não desconta delta de âncora

**Files:**
- Modify: `src/tdt/roteador.py:150-160` (bloco do piso em `_quadrante`)
- Test: `tests/test_roteador.py`

- [ ] **Step 1: Teste que falha:** topo com `fonte="ancora_sigla"`, score 0.85, ajuste de regras 0.70 → hoje `confianca_top1 = 0.15 < 0.20` → revisão; esperado: decide.
- [ ] **Step 2: Implementar** (1 linha na `_quadrante`):

```python
    confianca_top1 = (topo.score if topo.fonte == "ancora_sigla"
                      else topo.score - (ajustes or {}).get(topo.sigla, 0.0))
```

(piso segue intacto p/ não-ancorados — proteção 51F→FC87 preservada; teste de guarda existente de `piso_decisao` continua passando.)
- [ ] **Step 3:** PASS + gate individual (revert + `spOBS_C2.txt` se cair). Commit `fix(roteador): piso nao desconta ancora exata (P5 C2)`.

### Task 14 (se partição LP confirmar): C4 — seleção positiva por classe de estados do MM

**Files:**
- Modify: `src/tdt/ancoragem_sigla.py` (estender `desambiguar_variante`)
- Test: `tests/test_ancoragem_sigla.py`, `tests/casos_travados` (caso nominal 81U1)

- [ ] **Step 1:** Localizar parser de estágio do motor (`grep -n "estagio" src/tdt/motor_regras.py`) — reusar na composição (como `fase_da_sigla` é reusada).
- [ ] **Step 2: Teste que falha (nominal da spec):**

```python
def test_81_habilitada_decide_81u1_por_classe_de_estados():
    # rec: descrição normalizada "81 SUB FREQUENCIA E1 HABILITADA", âncora exata "81"
    # LP: variantes 81* reais (81/81E1/81IE1/81O1/81U1...) com os MMs da v8
    # esperado: decidido == "81U1" (ATIVACAO isola 81U*; estágio E1 fecha 81U1)
```

- [ ] **Step 3: Implementar** em `desambiguar_variante` (antes do fallback pai-exato): expandir TODAS as variantes da família ancorada a partir da LP (índice de família — mesmo `_indice_prefixo` de `expansao_candidatos`, importável); `est = detectar_estado(rec.descricoes.normalizada)`; se `est` e classe ≠ INDEFINIDO:

```python
    compat = [s for s in variantes_familia
              if classe_do_mm(mm_por_sigla.get(s)) == est.classe]
    if len(compat) > 1 and estagio_texto is not None:
        por_estagio = [s for s in compat if estagio_da_sigla(s) == estagio_texto]
        compat = por_estagio or compat
    if len(compat) == 1:
        return replace(rec, sigla_sinal=compat[0], status="decidido",
                       justificativa="variante por classe de estados do MM (C4)")
    if 1 < len(compat) < len(variantes_familia):
        # não decide, mas estreita a sugestão às compatíveis — o motivo
        # variante_ambigua (C1) segue; a UI mostra só as plausíveis
        sugestao = tuple(c for c in rec.candidatos if c.sigla.upper() in set(compat))
        return replace(rec, candidatos=sugestao or rec.candidatos)  # segue revisao
```

`mm_por_sigla`: dict construído da LP (campo `mm` da aba — o catálogo 2C completo NÃO é dependência, spec §4.2). C4 roda DENTRO do fluxo C1 (mesmo ponto do pipeline), decide via roteador/pós-roteador, nunca altera score de matching.
- [ ] **Step 4:** PASS (inclui teste C1 intacto: pai-exato continua o fallback). **Step 5: Gate individual** — revert + `spOBS_C4.txt` se cair. **Step 6:** Caso 81U1 entra no corpus travado (`tests/` — mesmo arquivo dos casos reais travados). **Step 7:** Commit `feat(roteador): C4 selecao positiva por classe de estados do MM (P5, caso 81U1)`.

---

## Fase 5 — Lista padrão, médio risco (2B, 2C-uso2, 2E, 2G, 2F)

### Task 15: 2B — FASES estruturada dos analógicos

**Files:**
- Modify: `src/tdt/dados/lista_padrao.py` (campo `fases` no `SinalPadrao`, coluna FASES da AnalogSignals)
- Modify: `src/tdt/motor_regras.py` (regra nova no registro `_REGRAS`)
- Test: `tests/test_lista_padrao.py`, `tests/test_motor_regras.py`

- [ ] **Step 1:** Loader: campo `fases: str | None = None` + mapping `"FASES": "fases"` na sheet AnalogSignals (62/62 preenchidas, medição 17/07). Teste de loader primeiro.
- [ ] **Step 2: Teste da regra que falha:** rec analógico com `eletrico.fase == "A"`; candidato cuja LP tem `fases == "L2"` → ajuste negativo; `fases == "L1 L2 L3"` → compatível (bônus); sem `fases` → `_ZERO`.
- [ ] **Step 3: Implementar** `r10_fase_lp` no padrão de `r3_fase` (`motor_regras.py:186-225`), com tradução `L1/L2/L3 → A/B/C`:

```python
_FASES_LP = {"L1": "A", "L2": "B", "L3": "C"}

def _fases_da_lp(valor: str) -> str:
    letras = sorted({_FASES_LP.get(t, t) for t in valor.upper().split()})
    return "".join(letras)  # "L1 L2 L3" -> "ABC"; "N" -> "N"

def r10_fase_lp(rec, cand, ctx, cfg):
    """FASES estruturada da AnalogSignals × fase extraída do texto (2B)."""
    if ctx.lista_padrao is None or ctx.eletrico is None or not ctx.eletrico.fase:
        return _ZERO
    sp = ctx.lista_padrao_por_sigla.get(cand.sigla.upper())  # ver Step 3b
    if sp is None or sp.categoria != "Analog" or not sp.fases:
        return _ZERO
    peso = cfg.pesos_regras["fase"]
    lp_fases = _fases_da_lp(sp.fases)
    if ctx.eletrico.fase in lp_fases or lp_fases == "ABC":
        return AjusteRegra(peso, f"fase LP: {sp.fases} compatível com {ctx.eletrico.fase}")
    return AjusteRegra(-peso, f"fase LP: {sp.fases} diverge de {ctx.eletrico.fase}")
```

Step 3b: conferir como o `Contexto` do motor acessa a LP (`grep -n "lista_padrao" src/tdt/motor_regras.py`) e usar o acesso existente (não criar caminho novo). Registrar a regra no registro `_REGRAS`.
- [ ] **Step 4:** PASS. **Step 5: Gate** + **verificação manual obrigatória** em lista real com analógicos (GTA) — o ledger registra que analógicos são excluídos do gate (follow-up SP-Pendencias-09jul Task 8); comparar contagem decidido/revisão dos analógicos antes/depois e amostrar 10 casos. **Step 6:** Commit `feat(regras): fase estruturada da LP p/ analogicos (2B)`.

### Task 16: 2C-uso2 — estados do catálogo MM como fallback do D2

**Files:**
- Modify: `src/tdt/semantica_estados.py` (`filtrar_por_estado`:126+ — fonte dos estados do candidato)
- Test: `tests/test_semantica_estados.py`

- [ ] **Step 1:** Ler `filtrar_por_estado` completo — identificar onde o par de estados do candidato vem de `sp.estados_brutos`/`sp.mm`.
- [ ] **Step 2: Teste que falha:** candidato cuja `estados_brutos` é None mas `mm` está no catálogo (Task 10) com par conhecido → o filtro passa a discriminá-lo (hoje: sem estados → não filtra).
- [ ] **Step 3: Implementar** fallback: `estados = sp.estados_brutos or catalogo_mm_estados.get(sp.mm)`. **Precedência (decisão spec §9.4): FUNÇÃO da aba SEMPRE prevalece; divergência entre os dois → aviso** (canal de avisos da Task 10) sem mudar o comportamento.
- [ ] **Step 4:** PASS + **gate individual** (muda quem o D2 filtra) — revert + `spOBS_2C2.txt` se cair. **Step 5:** Commit `feat(estados): catalogo MM como fallback do filtro D2 (2C-uso2)`.

### Task 17: 2E — diagnóstico MANUT_* (condicional)

**Files:**
- Create: `bench/diag_manut_lp.py`

- [ ] **Step 1:** Script: carregar as 33 linhas `MANUT_DiscreteSignals`/`Manut_AnalogSignals` (sigla, descrição); varrer as listas reais: contar sinais de entrada cuja descrição casa (âncora exata da sigla MANUT no texto OU fuzzy > 90 na descrição) e que hoje terminam em revisão/FP. Imprimir os casos.
- [ ] **Step 2:** ≥ 5 casos reais → abrir task de inclusão como 4ª fonte do catálogo (flag `origem="manut"`, gate individual) — adicionar ao plano na hora, com a mesma estrutura da Task 7. < 5 → arquivar: registrar contagem em `bench/resultados/spOBS_2E.txt` e no ledger ("2E medida, não incluída"). **Não incluir às cegas** (lição v5: mais candidatos = diluição).
- [ ] **Step 3:** Commit `docs(diag): mede aproveitamento das sheets MANUT (2E)`.

### Task 18: 2G — DMS Signal Explanation × mapa de Measurement Type (condicional)

**Files:**
- Create: `bench/diag_measurement_type.py`
- Modify (condicional): módulo do `_MEASUREMENT_TYPE_PT_EN` (`grep -rn "_MEASUREMENT_TYPE_PT_EN" src/`)

- [ ] **Step 1:** Script: ler a sheet `DMS Signal Explanation` (colunas `MatchingString`, `signalType`, `measurementType`, `phaseCode & sideMeasured`); cruzar com o mapa hardcoded `_MEASUREMENT_TYPE_PT_EN` (12 tipos): listar (a) divergências, (b) tipos da sheet sem entrada no mapa — o caso KMDF (observações item 16) deve aparecer em (b) resolvido pela sheet.
- [ ] **Step 2:** Se a sheet cobre mais: acrescentar as entradas FALTANTES ao mapa hardcoded (não trocar a arquitetura — mapa continua fonte, sheet vira referência documentada no comentário). Teste de não-regressão: os 12 mapeamentos atuais devolvem o MESMO resultado.
- [ ] **Step 3:** Suíte + verificação em lista real com analógicos (GTA). Sem gate (Measurement Type não entra no matching). **Step 4:** Commit `feat(engine): measurement types complementados pela DMS Signal Explanation (2G)`.

### Task 19: 2F — coluna EQUIPAMENTO dedicada (lacuna fluxo_dados.md)

**Files:**
- Modify: `src/tdt/analise/analise_colunas.py` (detecção da coluna rotulada EQUIPAMENTO)
- Modify: `src/tdt/normalizacao/estruturador.py` (consumir em ramo INDEPENDENTE — regra I2)
- Modify: `docs/fluxo_dados.md` (fechar a lacuna registrada)
- Test: `tests/test_analise_colunas.py`, `tests/test_estruturador.py`

- [ ] **Step 1: Teste que falha (analise_colunas):** rows com header "EQUIPAMENTO" e células com IDs (`52-1`, `89-2`) → `mapa.coluna_equipamento == <índice>`; coluna de descrição não é confundida (usar o padrão dos testes de detecção existentes).
- [ ] **Step 2: Teste que falha (estruturador):** linha com célula na coluna EQUIPAMENTO contendo `52-1` → registro sai com `eletrico.nome_equipamento == "52-1"`; sigla e módulo do MESMO registro não mudam (guarda I2 — replicar os 3 testes de independência de 87a347b).
- [ ] **Step 3: Implementar:** detecção por rótulo (mesmo mecanismo do rótulo de endereço/módulo em `analise_colunas.py:139,211`); no estruturador, ramo novo independente que preenche `nome_equipamento` só quando vazio (mesma política de `atribuir_id_por_registro`: nunca sobrescrever). A varredura de linha inteira existente CONTINUA (não é substituída — coexistência, CLAUDE.md regra 1); conflito coluna×varredura → motivo `equipamento_conflitante` existente.
- [ ] **Step 4:** PASS. **Step 5: Gate** (endereçamento não muda, mas equipamento alimenta whitelist D6 → roda gate; revert + `spOBS_2F.txt` se cair). Rodar também `scripts/relatorio_fluxo_real.py` na LVA (a lista da lacuna). **Step 6:** atualizar `docs/fluxo_dados.md` §Lacunas. **Step 7:** Commit `feat(analise): coluna EQUIPAMENTO dedicada, ramo independente (2F)`.

---

## Fase 6 — Closeout

### Task 20: Verificação final + ledger + DOX

- [ ] **Step 1:** Suíte completa: `PYTHONPATH=src python -m pytest` → 0 falhas.
- [ ] **Step 2:** Gate final: `pct >= baseline` (Task 0); anotar o pct final em `bench/resultados/spOBS_final.txt`.
- [ ] **Step 3:** `PYTHONPATH=src python scripts/relatorio_fluxo_real.py` → 0 PERDAS (LVA + CVA).
- [ ] **Step 4:** Reprocessar 1 lista real via UI (config novo — pesos calibrados agora efetivos) e comparar contagens decidido/revisão com a rodada pré-P4; diferenças são a correção esperada — registrar no ledger, não tratar como regressão.
- [ ] **Step 5:** Ledger `docs/AGENTS.md`: 1 linha por task fechada (P1, P3, P3b, P4, 2A, 2D, 2C, C1/C2/C4 com veredito do diagnóstico, 2B, 2E/2G condicionais, 2F) + follow-up novo: "preservar dado absorvido na fusão (P3b §9.6)".
- [ ] **Step 6:** DOX pass: reler cadeia `AGENTS.md` afetada (`docs/`, `src/tdt/`) e atualizar contratos que mudaram (`ListaPadraoADMS.de_para/catalogo_mm`, motivo `variante_ambigua`, coluna renomeada).
- [ ] **Step 7:** Commit `docs(ledger): closeout SP-OBS-17JUL`.

---

## Self-Review (cobertura vs spec)

- P1 §3 (display+edição+rename) → Task 3 ✓
- P2 §4: 2A → Tasks 7-8 ✓; 2B → Task 15 ✓; 2C uso1/uso2 → Tasks 10/16 ✓; 2D → Task 9 ✓; 2E → Task 17 (condicional) ✓; 2F → Task 19 ✓; 2G → Task 18 (condicional) ✓
- P3 §5 (núcleo + UI) → Tasks 4-5 ✓; P3b → Task 6 ✓
- P4 §6 (delta-only + migração + UI) → Tasks 1-2 ✓
- P5 §7: Fase 0 → Task 11 ✓; C1 → Task 12 ✓; C2 → Task 13 ✓; C3 → sem task (design depende do achado H3; se H3 confirmada, abrir task na hora — decisão da spec) ✓; C4 → Task 14 ✓
- Verificação §8 → Task 20 + gates individuais ✓

**Total:** 21 tasks (0–20). Ordem de risco: Fases 1-3 determinísticas/UI → Fase 4 roteador (diagnóstico primeiro, 1 correção por gate) → Fase 5 scoring/fontes → closeout.
